---
phase: 02-trailing-stop-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mutrade/engine/__init__.py
  - mutrade/engine/models.py
  - mutrade/engine/state_store.py
  - mutrade/engine/trailing_stop.py
  - tests/test_engine.py
  - tests/test_state_store.py
autonomous: true
requirements: [ENG-01, ENG-02, ENG-03, ENG-04, ENG-05]

must_haves:
  truths:
    - "tick()에 가격을 넣으면 고점이 갱신된다"
    - "고점 대비 threshold 이상 하락 시 SellSignal이 반환된다"
    - "threshold 미만 하락 시 SellSignal이 반환되지 않는다"
    - "state.json에 고점이 원자적으로 저장된다"
    - "state.json에서 고점을 복원하면 이전 고점부터 추적을 재개한다"
    - "config.toml에 없는 종목은 state.json에서 무시된다"
    - "첫 번째 tick에서는 매도 신호가 발생하지 않는다"
    - "종목별 개별 threshold가 기본값을 오버라이드한다"
  artifacts:
    - path: "mutrade/engine/models.py"
      provides: "SellSignal dataclass, SymbolState dataclass"
      exports: ["SellSignal", "SymbolState"]
    - path: "mutrade/engine/state_store.py"
      provides: "Atomic state.json read/write"
      exports: ["StateStore"]
    - path: "mutrade/engine/trailing_stop.py"
      provides: "TrailingStopEngine with tick() method"
      exports: ["TrailingStopEngine"]
    - path: "tests/test_engine.py"
      provides: "Engine behavior tests"
      min_lines: 100
    - path: "tests/test_state_store.py"
      provides: "State persistence tests"
      min_lines: 40
  key_links:
    - from: "mutrade/engine/trailing_stop.py"
      to: "mutrade/engine/state_store.py"
      via: "StateStore.save() called when peak updates"
      pattern: "self\\._store\\.save"
    - from: "mutrade/engine/trailing_stop.py"
      to: "mutrade/engine/models.py"
      via: "imports SellSignal, SymbolState"
      pattern: "from mutrade\\.engine\\.models import"
    - from: "mutrade/engine/state_store.py"
      to: "state.json"
      via: "tempfile + os.replace atomic write"
      pattern: "os\\.replace"
---

<objective>
트레일링 스탑 엔진 핵심 로직과 상태 저장소를 TDD로 구현한다.

Purpose: 가격 입력 -> 고점 갱신 -> 하락률 계산 -> 매도 신호 반환 파이프라인을 테스트 먼저 작성하여 모든 엣지 케이스를 커버한다.
Output: TrailingStopEngine, StateStore, SellSignal — 완전히 테스트된 엔진 코어
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trailing-stop-engine/02-CONTEXT.md

<interfaces>
<!-- Phase 1 산출물 — 엔진이 소비하는 타입과 함수 -->

From mutrade/config/loader.py:
```python
@dataclass(frozen=True)
class SymbolConfig:
    code: str           # 종목 코드 (예: "005930")
    name: str           # 종목명 (예: "삼성전자")
    threshold: float    # 트레일링 스탑 하락률 임계값 (예: 0.10 = 10%)

@dataclass(frozen=True)
class AppConfig:
    poll_interval: float
    default_threshold: float
    symbols: list[SymbolConfig]
    market_open_hour: int = 9
    market_open_minute: int = 0
    market_close_hour: int = 15
    market_close_minute: int = 20
```

From mutrade/kis/price_feed.py:
```python
def poll_prices(kis, config: AppConfig) -> dict[str, float]:
    """종목 코드 → 현재가 dict. 에러 종목은 제외."""
```

From tests/test_price_feed.py (테스트 패턴):
```python
# make_config() 헬퍼, patch("module.datetime") 패턴, MagicMock 사용
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 엔진 모델, 상태 저장소, 트레일링 스탑 엔진 (TDD)</name>
  <files>
    mutrade/engine/__init__.py,
    mutrade/engine/models.py,
    mutrade/engine/state_store.py,
    mutrade/engine/trailing_stop.py,
    tests/test_engine.py,
    tests/test_state_store.py
  </files>
  <read_first>
    mutrade/config/loader.py,
    mutrade/settings.py,
    mutrade/kis/price_feed.py,
    tests/test_price_feed.py,
    .planning/phases/02-trailing-stop-engine/02-CONTEXT.md
  </read_first>
  <behavior>
    ## StateStore 테스트 (tests/test_state_store.py)
    - Test 1: save() 후 load()하면 동일한 dict[str, SymbolState] 반환
    - Test 2: state.json 없을 때 load()는 빈 dict 반환
    - Test 3: save()는 tempfile + os.replace 원자적 쓰기 사용 (os.replace 호출 확인)
    - Test 4: save() 후 state.json 파일에 JSON이 정상 기록됨

    ## TrailingStopEngine 테스트 (tests/test_engine.py)
    - Test 1: 첫 tick — 새 종목의 가격이 고점으로 설정됨, SellSignal 없음
    - Test 2: 가격 상승 tick — 고점이 새 가격으로 갱신됨
    - Test 3: 가격 하락 (threshold 미만) — 고점 유지, SellSignal 없음
    - Test 4: 가격 하락 (threshold 이상) — SellSignal 반환 (symbol, price, peak, drop_pct 포함)
    - Test 5: 종목별 threshold — SymbolConfig.threshold=0.05인 종목이 5% 하락 시 SellSignal 발생
    - Test 6: 재시작 복원 — StateStore에서 로드한 고점으로 초기화, 이전 고점 기준 추적
    - Test 7: 첫 tick 안전장치 — 재시작 후 첫 tick에서 고점 아래 가격이어도 SellSignal 없음 (warm-up)
    - Test 8: 두 번째 tick부터 매도 신호 — warm-up 이후 threshold 이상 하락 시 SellSignal 발생
    - Test 9: config에 없는 종목은 state에서 무시 — load 시 config 기준 필터링
    - Test 10: config에 신규 추가 종목 — state에 없으면 첫 가격으로 초기화
    - Test 11: 고점 갱신 시 StateStore.save() 호출됨
    - Test 12: 고점 갱신 없으면 StateStore.save() 호출 안 됨
    - Test 13: 드라이런 모드에서 SellSignal.dry_run=True
  </behavior>
  <action>
    ## RED 단계: 테스트 먼저 작성

    ### 1. mutrade/engine/__init__.py
    빈 파일 생성.

    ### 2. mutrade/engine/models.py — 타입 정의
    ```python
    from dataclasses import dataclass

    @dataclass
    class SymbolState:
        code: str          # 종목 코드
        peak_price: float  # 고점 (high-water mark)
        warm: bool = False # 첫 tick 이후 True (warm-up 완료)

    @dataclass(frozen=True)
    class SellSignal:
        code: str          # 종목 코드
        name: str          # 종목명
        current_price: float
        peak_price: float
        drop_pct: float    # 하락률 (0.10 = 10%)
        threshold: float   # 적용된 임계값
        dry_run: bool      # 드라이런 여부
    ```

    ### 3. tests/test_state_store.py 작성
    `StateStore(path)` 클래스 테스트:
    - `save(states: dict[str, SymbolState])`: `tempfile.NamedTemporaryFile(mode="w", dir=path.parent, delete=False)` 로 JSON 작성 후 `os.replace(tmp.name, path)` 원자적 교체
    - `load() -> dict[str, SymbolState]`: 파일이 있으면 JSON 파싱하여 `dict[str, SymbolState]` 반환, 없으면 `{}`

    state.json 포맷:
    ```json
    {
      "005930": {"code": "005930", "peak_price": 76000.0, "warm": true},
      "000660": {"code": "000660", "peak_price": 190000.0, "warm": true}
    }
    ```

    ### 4. tests/test_engine.py 작성
    `TrailingStopEngine(symbols: list[SymbolConfig], store: StateStore, dry_run: bool = False)` 테스트.
    생성자에서 `store.load()`를 호출하고 config 기준으로 필터링 (D-02).
    config에 없는 종목은 무시, 신규 종목은 state가 없는 상태로 시작.

    핵심 메서드: `tick(prices: dict[str, float]) -> list[SellSignal]`
    - prices에 있는 각 종목에 대해:
      1. state가 없으면(신규): SymbolState(code, peak_price=price, warm=False) 생성, SellSignal 없음
      2. warm=False이면: warm=True로 갱신, 가격이 고점보다 높으면 고점 갱신, SellSignal 없음 (안전장치)
      3. warm=True이면:
         - price > peak → peak 갱신, store.save() 호출
         - drop_pct = (peak - price) / peak
         - drop_pct >= threshold → SellSignal 추가
      4. 고점이 갱신된 경우에만 store.save() 호출 (D: 상태 저장 빈도)

    테스트에서 StateStore는 MagicMock으로 대체하여 엔진 로직만 격리 테스트.

    ### 5. 테스트 실행 — RED 확인
    `python -m pytest tests/test_engine.py tests/test_state_store.py -x` → 모든 테스트 FAIL

    ## GREEN 단계: 최소 구현

    ### 6. mutrade/engine/state_store.py 구현
    ```python
    import json
    import os
    import tempfile
    from pathlib import Path
    from mutrade.engine.models import SymbolState

    class StateStore:
        def __init__(self, path: str | Path = "state.json"):
            self._path = Path(path)

        def load(self) -> dict[str, SymbolState]:
            if not self._path.exists():
                return {}
            with open(self._path, "r") as f:
                data = json.load(f)
            return {
                code: SymbolState(code=code, peak_price=v["peak_price"], warm=v.get("warm", True))
                for code, v in data.items()
            }

        def save(self, states: dict[str, SymbolState]) -> None:
            data = {
                code: {"code": s.code, "peak_price": s.peak_price, "warm": s.warm}
                for code, s in states.items()
            }
            self._path.parent.mkdir(parents=True, exist_ok=True)
            fd, tmp_path = tempfile.mkstemp(
                dir=self._path.parent, suffix=".tmp"
            )
            try:
                with os.fdopen(fd, "w") as f:
                    json.dump(data, f, indent=2)
                os.replace(tmp_path, self._path)
            except BaseException:
                os.unlink(tmp_path)
                raise
    ```

    ### 7. mutrade/engine/trailing_stop.py 구현
    ```python
    from loguru import logger
    from mutrade.config.loader import SymbolConfig
    from mutrade.engine.models import SellSignal, SymbolState
    from mutrade.engine.state_store import StateStore

    class TrailingStopEngine:
        def __init__(
            self,
            symbols: list[SymbolConfig],
            store: StateStore,
            dry_run: bool = False,
        ):
            self._symbols = {s.code: s for s in symbols}
            self._store = store
            self._dry_run = dry_run

            # state.json 로드 후 config 기준 필터링 (D-02)
            loaded = store.load()
            self._states: dict[str, SymbolState] = {
                code: loaded[code]
                for code in self._symbols
                if code in loaded
            }

        def tick(self, prices: dict[str, float]) -> list[SellSignal]:
            signals: list[SellSignal] = []
            peak_updated = False

            for code, price in prices.items():
                if code not in self._symbols:
                    continue
                sym = self._symbols[code]

                if code not in self._states:
                    # 신규 종목: 첫 가격을 고점으로 (D-01)
                    self._states[code] = SymbolState(
                        code=code, peak_price=price, warm=False
                    )
                    peak_updated = True
                    logger.info(
                        "New symbol {} ({}): initial peak={:,.0f}",
                        code, sym.name, price,
                    )
                    continue

                state = self._states[code]

                if not state.warm:
                    # 첫 tick 안전장치: warm-up
                    if price > state.peak_price:
                        state.peak_price = price
                        peak_updated = True
                    state.warm = True
                    continue

                # 고점 갱신
                if price > state.peak_price:
                    state.peak_price = price
                    peak_updated = True
                    logger.debug(
                        "{} ({}) new peak: {:,.0f}",
                        code, sym.name, price,
                    )

                # 하락률 계산
                if state.peak_price > 0:
                    drop_pct = (state.peak_price - price) / state.peak_price
                else:
                    drop_pct = 0.0

                if drop_pct >= sym.threshold:
                    signal = SellSignal(
                        code=code,
                        name=sym.name,
                        current_price=price,
                        peak_price=state.peak_price,
                        drop_pct=drop_pct,
                        threshold=sym.threshold,
                        dry_run=self._dry_run,
                    )
                    signals.append(signal)
                    logger.warning(
                        "[{}] 매도 신호: {} ({}) 현재가={:,.0f} 고점={:,.0f} "
                        "하락률={:.1%} 임계값={:.1%}",
                        "DRY-RUN" if self._dry_run else "LIVE",
                        code, sym.name, price,
                        state.peak_price, drop_pct, sym.threshold,
                    )

            if peak_updated:
                self._store.save(self._states)

            return signals

        @property
        def states(self) -> dict[str, SymbolState]:
            return dict(self._states)
    ```

    ### 8. 테스트 실행 — GREEN 확인
    `python -m pytest tests/test_engine.py tests/test_state_store.py -x`

    ## REFACTOR 단계
    코드 정리 필요 시 수행. 테스트 재실행하여 green 유지 확인.
  </action>
  <verify>
    <automated>cd /Users/sean/Study/MuTrade/MuTrade && python -m pytest tests/test_engine.py tests/test_state_store.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - mutrade/engine/models.py contains `class SellSignal` and `class SymbolState`
    - mutrade/engine/models.py contains `dry_run: bool`
    - mutrade/engine/state_store.py contains `class StateStore`
    - mutrade/engine/state_store.py contains `os.replace(`
    - mutrade/engine/state_store.py contains `tempfile.mkstemp(`
    - mutrade/engine/trailing_stop.py contains `class TrailingStopEngine`
    - mutrade/engine/trailing_stop.py contains `def tick(self, prices: dict[str, float]) -> list[SellSignal]`
    - mutrade/engine/trailing_stop.py contains `state.warm`
    - mutrade/engine/trailing_stop.py contains `self._store.save(`
    - tests/test_engine.py contains at least 10 test functions (test_)
    - tests/test_state_store.py contains at least 3 test functions (test_)
    - `python -m pytest tests/test_engine.py tests/test_state_store.py -v` exits 0 with all tests passing
    - `python -m pytest tests/ -v` exits 0 (no regressions in Phase 1 tests)
  </acceptance_criteria>
  <done>
    엔진 코어가 TDD로 구현됨: 고점 추적, 원자적 상태 저장/복원, 하락률 매도 신호, 종목별 임계값, warm-up 안전장치, 드라이런 플래그.
    모든 테스트 green, Phase 1 테스트 regression 없음.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_engine.py tests/test_state_store.py -v` — 모든 엔진 테스트 통과
2. `python -m pytest tests/ -v` — 전체 테스트 스위트 통과 (Phase 1 포함)
3. `grep -r "class TrailingStopEngine" mutrade/` — 엔진 클래스 존재 확인
4. `grep -r "os.replace" mutrade/engine/state_store.py` — 원자적 쓰기 확인
</verification>

<success_criteria>
- TrailingStopEngine.tick()이 고점을 추적하고 threshold 이상 하락 시 SellSignal을 반환한다
- StateStore가 tempfile+os.replace로 원자적 state.json 읽기/쓰기를 수행한다
- 첫 tick (warm=False) 에서는 절대 SellSignal을 반환하지 않는다
- config.toml에 없는 종목은 state.json에서 무시된다
- 모든 테스트가 통과하며 Phase 1 테스트에 regression이 없다
</success_criteria>

<output>
After completion, create `.planning/phases/02-trailing-stop-engine/02-01-SUMMARY.md`
</output>
