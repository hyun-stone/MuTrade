---
phase: 02-trailing-stop-engine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - mutrade/settings.py
  - mutrade/monitor/scheduler.py
  - mutrade/main.py
  - tests/test_scheduler.py
  - tests/test_settings.py
autonomous: true
requirements: [ENG-01, ENG-02, ENG-03, ENG-04, ENG-05]

must_haves:
  truths:
    - "Settings에 DRY_RUN 필드가 있고 .env에서 읽힌다"
    - "스케줄러 폴링 루프가 poll_prices 결과를 engine.tick()에 전달한다"
    - "engine.tick()이 반환한 SellSignal을 로그에 기록한다"
    - "main.py가 StateStore와 TrailingStopEngine을 초기화하여 스케줄러에 전달한다"
    - "재시작 후 state.json에서 고점을 복원하여 추적을 재개한다"
  artifacts:
    - path: "mutrade/settings.py"
      provides: "DRY_RUN 필드 추가"
      contains: "dry_run"
    - path: "mutrade/monitor/scheduler.py"
      provides: "engine.tick() 호출 통합"
      contains: "engine.tick"
    - path: "mutrade/main.py"
      provides: "TrailingStopEngine 초기화 및 와이어링"
      contains: "TrailingStopEngine"
  key_links:
    - from: "mutrade/monitor/scheduler.py"
      to: "mutrade/engine/trailing_stop.py"
      via: "engine.tick(prices) call in poll loop"
      pattern: "engine\\.tick\\(prices\\)"
    - from: "mutrade/main.py"
      to: "mutrade/engine/trailing_stop.py"
      via: "TrailingStopEngine instantiation"
      pattern: "TrailingStopEngine\\("
    - from: "mutrade/main.py"
      to: "mutrade/engine/state_store.py"
      via: "StateStore instantiation"
      pattern: "StateStore\\("
---

<objective>
트레일링 스탑 엔진을 기존 폴링 루프와 봇 엔트리포인트에 통합한다.

Purpose: Plan 01에서 만든 엔진 코어를 실제 폴링 루프에 연결하여, 가격 폴링 → 고점 추적 → 매도 신호 로깅 파이프라인을 완성한다.
Output: Settings에 DRY_RUN 추가, scheduler에 engine.tick() 호출 통합, main.py에 엔진 초기화 와이어링
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-trailing-stop-engine/02-CONTEXT.md
@.planning/phases/02-trailing-stop-engine/02-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성되는 인터페이스 -->

From mutrade/engine/models.py:
```python
@dataclass
class SymbolState:
    code: str
    peak_price: float
    warm: bool = False

@dataclass(frozen=True)
class SellSignal:
    code: str
    name: str
    current_price: float
    peak_price: float
    drop_pct: float
    threshold: float
    dry_run: bool
```

From mutrade/engine/state_store.py:
```python
class StateStore:
    def __init__(self, path: str | Path = "state.json"): ...
    def load(self) -> dict[str, SymbolState]: ...
    def save(self, states: dict[str, SymbolState]) -> None: ...
```

From mutrade/engine/trailing_stop.py:
```python
class TrailingStopEngine:
    def __init__(self, symbols: list[SymbolConfig], store: StateStore, dry_run: bool = False): ...
    def tick(self, prices: dict[str, float]) -> list[SellSignal]: ...
    @property
    def states(self) -> dict[str, SymbolState]: ...
```

<!-- 기존 코드 — 수정 대상 -->

From mutrade/settings.py:
```python
class Settings(BaseSettings):
    kis_id: str = Field(..., alias="KIS_ID")
    kis_account: str = Field(..., alias="KIS_ACCOUNT")
    kis_appkey: str = Field(..., alias="KIS_APPKEY")
    kis_secretkey: str = Field(..., alias="KIS_SECRETKEY")
    kis_mock: bool = Field(False, alias="KIS_MOCK")
    # ... virtual credentials ...
```

From mutrade/monitor/scheduler.py:
```python
def create_poll_session(kis, config: AppConfig):
    def run_session():
        # ... KRX check, poll loop ...
        prices = poll_prices(kis, config)
        # Phase 2 에서 트레일링 스탑 엔진이 prices 를 소비한다
        time.sleep(config.poll_interval)
    return run_session

def start_scheduler(kis, config: AppConfig) -> None: ...
```

From mutrade/main.py:
```python
def main() -> None:
    settings = Settings()
    config = load_config()
    kis = create_kis_client(settings)
    start_scheduler(kis, config)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings에 DRY_RUN 필드 추가</name>
  <files>mutrade/settings.py, tests/test_settings.py</files>
  <read_first>
    mutrade/settings.py,
    tests/test_settings.py
  </read_first>
  <action>
    ## mutrade/settings.py 수정

    Settings 클래스에 `dry_run` 필드 추가. `KIS_MOCK=true`이면 자동으로 `dry_run=True`로 설정하는 로직 포함 (Claude discretion: KIS_MOCK=true + DRY_RUN=false는 의미 없음 — 모의투자에서 실매도 불가이므로 KIS_MOCK=true이면 dry_run 강제 활성화).

    `kis_mock` 관련 model_validator 아래에 추가:
    ```python
    # 드라이런 모드 (Phase 2 — 트레일링 스탑 엔진)
    dry_run: bool = Field(False, alias="DRY_RUN")
    ```

    기존 `validate_virtual_credentials` validator 수정 — 마지막에 추가:
    ```python
    @model_validator(mode="after")
    def validate_virtual_credentials(self) -> "Settings":
        """KIS_MOCK=true 일 때 가상 계좌 자격증명이 모두 존재하는지 검증."""
        if self.kis_mock:
            missing = []
            if not self.kis_virtual_id:
                missing.append("KIS_VIRTUAL_ID")
            if not self.kis_virtual_account:
                missing.append("KIS_VIRTUAL_ACCOUNT")
            if not self.kis_virtual_appkey:
                missing.append("KIS_VIRTUAL_APPKEY")
            if not self.kis_virtual_secretkey:
                missing.append("KIS_VIRTUAL_SECRETKEY")
            if missing:
                raise ValueError(
                    f"KIS_MOCK=true requires virtual credentials: {', '.join(missing)}"
                )
            # KIS_MOCK=true 이면 dry_run 강제 활성화
            if not self.dry_run:
                object.__setattr__(self, "dry_run", True)
        return self
    ```

    ## tests/test_settings.py 수정

    기존 테스트 끝에 추가:
    ```python
    class TestDryRun:
        def test_dry_run_default_false(self):
            """DRY_RUN 기본값은 False."""
            s = Settings(
                KIS_ID="id", KIS_ACCOUNT="acc",
                KIS_APPKEY="key", KIS_SECRETKEY="secret",
            )
            assert s.dry_run is False

        def test_dry_run_explicit_true(self):
            """DRY_RUN=true 명시 시 활성화."""
            s = Settings(
                KIS_ID="id", KIS_ACCOUNT="acc",
                KIS_APPKEY="key", KIS_SECRETKEY="secret",
                DRY_RUN=True,
            )
            assert s.dry_run is True

        def test_kis_mock_forces_dry_run(self):
            """KIS_MOCK=true이면 DRY_RUN이 자동으로 True."""
            s = Settings(
                KIS_ID="id", KIS_ACCOUNT="acc",
                KIS_APPKEY="key", KIS_SECRETKEY="secret",
                KIS_MOCK=True,
                KIS_VIRTUAL_ID="vid", KIS_VIRTUAL_ACCOUNT="vacc",
                KIS_VIRTUAL_APPKEY="vkey", KIS_VIRTUAL_SECRETKEY="vsec",
                DRY_RUN=False,
            )
            assert s.dry_run is True
    ```
  </action>
  <verify>
    <automated>cd /Users/sean/Study/MuTrade/MuTrade && python -m pytest tests/test_settings.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - mutrade/settings.py contains `dry_run: bool = Field(False, alias="DRY_RUN")`
    - mutrade/settings.py contains `object.__setattr__(self, "dry_run", True)`
    - tests/test_settings.py contains `class TestDryRun`
    - tests/test_settings.py contains `test_kis_mock_forces_dry_run`
    - `python -m pytest tests/test_settings.py -v` exits 0
  </acceptance_criteria>
  <done>
    Settings에 DRY_RUN 필드가 추가되고, KIS_MOCK=true 시 자동 활성화됨. 테스트 통과.
  </done>
</task>

<task type="auto">
  <name>Task 2: 스케줄러와 main.py에 엔진 통합</name>
  <files>mutrade/monitor/scheduler.py, mutrade/main.py, tests/test_scheduler.py</files>
  <read_first>
    mutrade/monitor/scheduler.py,
    mutrade/main.py,
    tests/test_scheduler.py,
    mutrade/engine/trailing_stop.py,
    mutrade/engine/models.py,
    mutrade/engine/state_store.py
  </read_first>
  <action>
    ## mutrade/monitor/scheduler.py 수정

    `create_poll_session` 시그니처에 `engine` 파라미터 추가. `start_scheduler`에도 동일 추가.

    변경 내용:
    ```python
    from mutrade.engine.trailing_stop import TrailingStopEngine

    def create_poll_session(kis, config: AppConfig, engine: TrailingStopEngine):
        def run_session():
            today = datetime.now(KST).date()
            if not is_krx_trading_day(today):
                logger.info("Today ({}) is not a KRX trading day. Skipping.", today)
                return

            logger.info(
                "Market session started. Monitoring {} symbols.", len(config.symbols)
            )
            for s in config.symbols:
                logger.info(
                    "  {} ({}) threshold={:.1%}", s.code, s.name, s.threshold
                )

            # 엔진 상태 로깅 (재시작 후 고점 복원 확인)
            for code, state in engine.states.items():
                logger.info(
                    "  {} peak={:,.0f} warm={}", code, state.peak_price, state.warm
                )

            close_minutes = config.market_close_hour * 60 + config.market_close_minute

            while True:
                now_kst = datetime.now(KST)
                current_minutes = now_kst.hour * 60 + now_kst.minute
                if current_minutes >= close_minutes:
                    logger.info(
                        "Market session ended ({:02d}:{:02d} KST).",
                        config.market_close_hour, config.market_close_minute,
                    )
                    break

                prices = poll_prices(kis, config)

                # 트레일링 스탑 엔진 tick
                signals = engine.tick(prices)
                for sig in signals:
                    logger.warning(
                        "[{}] SELL SIGNAL: {} ({}) price={:,.0f} peak={:,.0f} "
                        "drop={:.1%} threshold={:.1%}",
                        "DRY-RUN" if sig.dry_run else "LIVE",
                        sig.code, sig.name, sig.current_price,
                        sig.peak_price, sig.drop_pct, sig.threshold,
                    )

                logger.info(
                    "Polled {} symbols: {}",
                    len(prices),
                    {k: f"{v:,.0f}" for k, v in prices.items()},
                )

                time.sleep(config.poll_interval)

        return run_session

    def start_scheduler(kis, config: AppConfig, engine: TrailingStopEngine) -> None:
        scheduler = BlockingScheduler(timezone="Asia/Seoul")
        poll_session = create_poll_session(kis, config, engine)

        scheduler.add_job(
            poll_session,
            CronTrigger(
                day_of_week="mon-fri",
                hour=config.market_open_hour,
                minute=config.market_open_minute,
                timezone="Asia/Seoul",
            ),
            id="market_poll",
            name="KIS Market Price Poll",
        )

        logger.info(
            "Scheduler ready. Next trigger: Mon-Fri {:02d}:{:02d} KST.",
            config.market_open_hour, config.market_open_minute,
        )

        try:
            scheduler.start()
        except (KeyboardInterrupt, SystemExit):
            logger.info("Scheduler stopped by user.")
    ```

    ## mutrade/main.py 수정

    `main()` 에 StateStore, TrailingStopEngine 초기화 추가:
    ```python
    from mutrade.engine.state_store import StateStore
    from mutrade.engine.trailing_stop import TrailingStopEngine

    def main() -> None:
        # ... (기존 loguru 설정, settings/config/kis 로드 동일) ...

        # 트레일링 스탑 엔진 초기화
        store = StateStore(path="state.json")
        engine = TrailingStopEngine(
            symbols=config.symbols,
            store=store,
            dry_run=settings.dry_run,
        )
        logger.info(
            "Trailing stop engine initialized. dry_run={}, {} symbols tracked",
            settings.dry_run, len(engine.states),
        )

        # 스케줄러 시작 (엔진 전달)
        start_scheduler(kis, config, engine)
    ```

    ## tests/test_scheduler.py 수정

    기존 테스트를 engine 파라미터에 맞게 업데이트. `create_poll_session(kis, config)` 호출을 `create_poll_session(kis, config, engine)` 으로 변경.

    engine mock 생성:
    ```python
    def make_engine_mock():
        engine = MagicMock()
        engine.tick.return_value = []
        engine.states = {}
        return engine
    ```

    기존 4개 테스트에 engine mock 추가. 신규 테스트 추가:
    ```python
    def test_poll_session_calls_engine_tick_with_prices(self):
        """폴링 후 engine.tick(prices) 가 호출되어야 한다."""
        # poll_prices가 가격을 반환하고 → engine.tick(prices) 호출 확인
        # 2번째 루프에서 장 마감으로 종료

    def test_poll_session_logs_sell_signals(self):
        """engine.tick()이 SellSignal을 반환하면 SELL SIGNAL 로그가 출력되어야 한다."""
        # engine.tick.return_value = [SellSignal(...)]
        # caplog 또는 patch로 logger.warning 호출 확인
    ```
  </action>
  <verify>
    <automated>cd /Users/sean/Study/MuTrade/MuTrade && python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - mutrade/monitor/scheduler.py contains `engine: TrailingStopEngine` in create_poll_session signature
    - mutrade/monitor/scheduler.py contains `signals = engine.tick(prices)`
    - mutrade/monitor/scheduler.py contains `SELL SIGNAL`
    - mutrade/main.py contains `from mutrade.engine.state_store import StateStore`
    - mutrade/main.py contains `from mutrade.engine.trailing_stop import TrailingStopEngine`
    - mutrade/main.py contains `StateStore(path="state.json")`
    - mutrade/main.py contains `TrailingStopEngine(`
    - mutrade/main.py contains `dry_run=settings.dry_run`
    - tests/test_scheduler.py contains `engine.tick`
    - `python -m pytest tests/ -v` exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    스케줄러 폴링 루프가 engine.tick()을 호출하고 SellSignal을 로깅한다.
    main.py가 StateStore와 TrailingStopEngine을 초기화하여 스케줄러에 전달한다.
    전체 테스트 스위트 통과.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v` — 전체 테스트 스위트 통과 (Phase 1 + Phase 2 모두)
2. `grep -r "engine.tick" mutrade/monitor/scheduler.py` — 엔진 호출 확인
3. `grep -r "TrailingStopEngine" mutrade/main.py` — 엔진 초기화 확인
4. `grep -r "dry_run" mutrade/settings.py` — DRY_RUN 설정 확인
5. `grep "SELL SIGNAL" mutrade/monitor/scheduler.py` — 매도 신호 로깅 확인
</verification>

<success_criteria>
- Settings.dry_run 필드가 .env에서 DRY_RUN을 읽고, KIS_MOCK=true 시 자동 활성화된다
- 스케줄러 폴링 루프가 poll_prices → engine.tick → SellSignal 로깅 파이프라인을 완성한다
- main.py가 StateStore("state.json")과 TrailingStopEngine을 초기화하고 스케줄러에 전달한다
- 전체 테스트 스위트 (Phase 1 + Phase 2) 가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/02-trailing-stop-engine/02-02-SUMMARY.md`
</output>
