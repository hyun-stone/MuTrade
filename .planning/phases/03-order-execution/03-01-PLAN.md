---
phase: 03-order-execution
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mutrade/executor/__init__.py
  - mutrade/executor/order_executor.py
  - tests/test_order_executor.py
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04]

must_haves:
  truths:
    - "OrderExecutor.execute(signal)가 kis.account().sell(market='KRX', symbol=code, price=None, qty=orderable)를 호출한다"
    - "매도 수량은 balance.stock(code).orderable 값을 그대로 사용한다"
    - "동일 종목에 대해 두 번째 execute 호출은 sell()을 호출하지 않는다 (SELL_PENDING)"
    - "주문 성공 후 daily_orders().order(order)로 체결 확인을 시도한다"
    - "dry_run=True인 SellSignal은 실제 주문 없이 로그만 출력한다"
    - "주문 실패 시 SELL_PENDING 플래그가 해제된다"
  artifacts:
    - path: "mutrade/executor/__init__.py"
      provides: "패키지 마커"
    - path: "mutrade/executor/order_executor.py"
      provides: "OrderExecutor 클래스 — execute(), confirm_fill()"
      exports: ["OrderExecutor"]
    - path: "tests/test_order_executor.py"
      provides: "EXEC-01~04 단위 테스트 (mock 기반)"
      min_lines: 80
  key_links:
    - from: "mutrade/executor/order_executor.py"
      to: "mutrade/engine/models.py"
      via: "SellSignal import"
      pattern: "from mutrade\\.engine\\.models import SellSignal"
    - from: "mutrade/executor/order_executor.py"
      to: "pykis"
      via: "kis.account().sell() 호출"
      pattern: "acc\\.sell\\("
---

<objective>
TDD로 OrderExecutor 핵심 로직을 구현한다: 시장가 매도 주문, 매도 가능 수량 조회, SELL_PENDING 중복 방지, 체결 확인 폴링.

Purpose: Phase 2 SellSignal을 실제 KIS 매도 주문으로 전환하는 핵심 실행기를 mock 기반 TDD로 구축한다.
Output: mutrade/executor/order_executor.py (OrderExecutor 클래스), tests/test_order_executor.py (단위 테스트)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-order-execution/03-RESEARCH.md

<interfaces>
<!-- SellSignal: OrderExecutor가 소비하는 입력 인터페이스 -->
From mutrade/engine/models.py:
```python
@dataclass(frozen=True)
class SellSignal:
    code: str               # 종목 코드
    name: str               # 종목명
    current_price: float    # 현재가
    peak_price: float       # 고점
    drop_pct: float         # 하락률 (0.10 = 10%)
    threshold: float        # 적용된 임계값
    dry_run: bool           # 드라이런 여부 (True이면 실제 주문 없음)
```

From tests/test_engine.py (테스트 패턴 참고):
```python
def make_store(states=None) -> MagicMock:
    store = MagicMock(spec=StateStore)
    store.load.return_value = states or {}
    return store
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OrderExecutor TDD — RED-GREEN-REFACTOR</name>
  <files>mutrade/executor/__init__.py, mutrade/executor/order_executor.py, tests/test_order_executor.py</files>
  <read_first>
    - mutrade/engine/models.py (SellSignal 인터페이스 — execute()의 입력)
    - tests/test_engine.py (기존 TDD 패턴 — MagicMock 사용법, 픽스처 헬퍼 구조)
    - .planning/phases/03-order-execution/03-RESEARCH.md (PyKis API 패턴 — Pattern 1~5)
  </read_first>
  <behavior>
    ## EXEC-01: 시장가 매도 주문
    - Test: execute(signal)가 acc.sell(market="KRX", symbol="005930", price=None, qty=10)을 호출한다
    - Test: dry_run=True인 signal에 대해 acc.sell()이 호출되지 않는다

    ## EXEC-02: 매도 가능 수량
    - Test: balance.stock(code).orderable 값(예: Decimal(10))이 qty로 전달된다
    - Test: balance.stock(code)가 None이면 sell() 호출 없이 리턴 + 경고 로그
    - Test: orderable이 0이면 sell() 호출 없이 리턴 + 경고 로그

    ## EXEC-03: SELL_PENDING 중복 방지
    - Test: 첫 execute는 sell() 호출, 같은 code로 두 번째 execute는 sell() 미호출
    - Test: 주문 실패(sell()에서 예외) 시 _pending에서 code 제거됨 → 재시도 가능
    - Test: confirm_fill 완료 후 _pending에서 code 제거됨 → 재시도 가능

    ## EXEC-04: 체결 확인
    - Test: sell() 성공 후 daily_orders().order(order)가 호출된다
    - Test: daily_orders()에서 record를 찾으면 executed_quantity 로그 출력
    - Test: max_attempts 초과 시 경고 로그 + _pending에서 code 제거
  </behavior>
  <action>
    **Phase: RED** — tests/test_order_executor.py 작성 먼저.

    `mutrade/executor/__init__.py`를 빈 파일로 생성.

    `tests/test_order_executor.py` 테스트 구조:

    ```python
    from unittest.mock import MagicMock, patch, call
    from decimal import Decimal
    import pytest
    from mutrade.engine.models import SellSignal
    from mutrade.executor.order_executor import OrderExecutor

    def make_signal(code="005930", name="삼성전자", dry_run=False) -> SellSignal:
        return SellSignal(
            code=code, name=name, current_price=70000.0,
            peak_price=80000.0, drop_pct=0.125, threshold=0.10,
            dry_run=dry_run,
        )

    def make_kis_mock(orderable=10):
        """PyKis mock — account().balance("KR").stock(code).orderable 체인."""
        kis = MagicMock()
        acc = MagicMock()
        balance = MagicMock()
        stock_item = MagicMock()
        stock_item.orderable = orderable
        balance.stock.return_value = stock_item
        acc.balance.return_value = balance
        order = MagicMock()
        order.branch = "00"
        order.number = "12345"
        acc.sell.return_value = order
        # daily_orders mock
        daily = MagicMock()
        record = MagicMock()
        record.executed_quantity = orderable
        record.pending_quantity = 0
        record.price = 70000.0
        daily.order.return_value = record
        acc.daily_orders.return_value = daily
        kis.account.return_value = acc
        return kis, acc, order
    ```

    테스트 케이스 (각각 독립적 함수):
    1. `test_market_sell_called` — execute(signal) 후 acc.sell(market="KRX", symbol="005930", price=None, qty=10) 호출 확인
    2. `test_dry_run_skips_sell` — dry_run=True signal → acc.sell 미호출
    3. `test_orderable_qty_used` — orderable=25로 mock → qty=25로 sell 호출 확인
    4. `test_no_stock_skips_sell` — balance.stock() returns None → sell 미호출
    5. `test_zero_orderable_skips` — orderable=0 → sell 미호출
    6. `test_sell_pending_blocks_duplicate` — execute 2회 → sell 1회만 호출
    7. `test_sell_failure_clears_pending` — sell() raises KisAPIError → _pending에서 제거 확인 (execute 재호출 시 sell 재시도)
    8. `test_fill_confirmed` — sell 후 daily_orders().order() 호출 확인
    9. `test_fill_timeout_clears_pending` — daily_orders().order() returns None 반복 → _pending에서 code 제거
    10. `test_confirm_fill_after_success_clears_pending` — 체결 확인 후 같은 code로 다시 execute 가능

    confirm_fill에서 `time.sleep`은 `@patch("mutrade.executor.order_executor.time.sleep")` 로 건너뛴다.

    모든 테스트 실행: `pytest tests/test_order_executor.py -x` — 전부 FAIL (RED).

    **Phase: GREEN** — mutrade/executor/order_executor.py 구현.

    ```python
    """
    mutrade/executor/order_executor.py

    매도 주문 실행기 — SellSignal을 KIS 시장가 매도 주문으로 전환.

    핵심 흐름:
      1. SELL_PENDING 중복 확인 (EXEC-03)
      2. 매도 가능 수량 조회 — balance.stock(code).orderable (EXEC-02)
      3. 시장가 매도 주문 — account.sell(market="KRX", price=None) (EXEC-01)
      4. 체결 확인 — daily_orders().order(order) 폴링 (EXEC-04)
    """
    import time

    from loguru import logger
    from pykis import PyKis, KisAPIError

    from mutrade.engine.models import SellSignal


    class OrderExecutor:
        def __init__(self, kis: PyKis, dry_run: bool = False):
            self._kis = kis
            self._dry_run = dry_run
            self._pending: set[str] = set()

        def execute(self, signal: SellSignal) -> None:
            if signal.dry_run or self._dry_run:
                logger.info(
                    "[DRY-RUN] 매도 주문 시뮬레이션: {} ({})",
                    signal.code, signal.name,
                )
                return

            if signal.code in self._pending:
                logger.warning(
                    "SELL_PENDING 중복 방지: {} — 이미 주문 진행 중",
                    signal.code,
                )
                return

            self._pending.add(signal.code)
            try:
                self._submit_order(signal)
            except Exception as e:
                logger.error("주문 실패 {}: {}", signal.code, e)
                self._pending.discard(signal.code)

        def _submit_order(self, signal: SellSignal) -> None:
            acc = self._kis.account()
            balance = acc.balance("KR")
            stock = balance.stock(signal.code)

            if stock is None or stock.orderable <= 0:
                logger.warning(
                    "매도 불가 {}: 잔고 없음 또는 orderable=0",
                    signal.code,
                )
                self._pending.discard(signal.code)
                return

            qty = stock.orderable
            order = acc.sell(
                market="KRX",
                symbol=signal.code,
                price=None,
                qty=qty,
            )
            logger.warning(
                "[LIVE] 매도 주문 제출: {} ({}) qty={} 주문번호={}",
                signal.code, signal.name, qty, order.number,
            )
            self._confirm_fill(acc, order, signal.code)

        def _confirm_fill(
            self,
            acc,
            order,
            symbol: str,
            max_attempts: int = 5,
            interval_sec: float = 3.0,
        ) -> None:
            for attempt in range(max_attempts):
                time.sleep(interval_sec)
                try:
                    daily = acc.daily_orders()
                    record = daily.order(order)
                    if record is not None:
                        logger.info(
                            "체결 확인 {}: 체결수량={} 미체결수량={} 체결단가={}",
                            symbol,
                            record.executed_quantity,
                            record.pending_quantity,
                            record.price,
                        )
                        self._pending.discard(symbol)
                        return
                except KisAPIError as e:
                    logger.warning(
                        "체결 확인 API 오류 (attempt {}): {}",
                        attempt + 1, e,
                    )
            logger.warning(
                "체결 확인 시간 초과: {} — SELL_PENDING 해제", symbol,
            )
            self._pending.discard(symbol)
    ```

    테스트 실행: `pytest tests/test_order_executor.py -x` — 전부 PASS (GREEN).

    **Phase: REFACTOR** — 필요 시 정리. 불필요한 코드 없으면 스킵.

    커밋: RED → `test(03-01): add failing OrderExecutor tests`, GREEN → `feat(03-01): implement OrderExecutor with sell/pending/fill`, REFACTOR (if any) → `refactor(03-01): clean up OrderExecutor`.
  </action>
  <verify>
    <automated>pytest tests/test_order_executor.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `mutrade/executor/__init__.py` 파일 존재
    - `mutrade/executor/order_executor.py`에 `class OrderExecutor` 정의됨
    - `OrderExecutor.__init__`이 `kis: PyKis` 와 `dry_run: bool = False` 파라미터를 가짐
    - `OrderExecutor.execute`가 `signal: SellSignal` 파라미터를 가짐
    - `OrderExecutor._pending`이 `set[str]` 타입
    - `_submit_order`에서 `acc.sell(market="KRX", symbol=signal.code, price=None, qty=qty)` 호출
    - `_submit_order`에서 `acc.balance("KR")` 후 `balance.stock(signal.code).orderable` 사용
    - `_confirm_fill`에서 `acc.daily_orders().order(order)` 호출
    - `tests/test_order_executor.py`에 최소 8개 테스트 함수 존재
    - `pytest tests/test_order_executor.py -x` 전체 통과
    - `pytest tests/ -x` 전체 통과 (기존 51개 + 신규 테스트)
  </acceptance_criteria>
  <done>OrderExecutor가 TDD로 구현되고 모든 EXEC-01~04 요구사항에 대한 단위 테스트가 통과한다. SELL_PENDING 중복 방지, 매도 가능 수량 조회, 체결 확인 폴링이 mock 기반으로 검증됨.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_order_executor.py -x -v` — OrderExecutor 단위 테스트 전체 통과
- `pytest tests/ -x -q` — 전체 테스트 스위트 통과 (기존 + 신규)
- `grep -n "class OrderExecutor" mutrade/executor/order_executor.py` — 클래스 존재 확인
- `grep -n "def execute" mutrade/executor/order_executor.py` — execute 메서드 존재 확인
- `grep -n "_pending" mutrade/executor/order_executor.py` — SELL_PENDING set 존재 확인
</verification>

<success_criteria>
1. OrderExecutor.execute(signal)가 PyKis account.sell(market="KRX", price=None)을 호출한다
2. 매도 수량으로 balance.stock(code).orderable 값을 사용한다
3. 동일 종목 두 번째 execute 호출이 sell()을 호출하지 않는다
4. 체결 확인으로 daily_orders().order()를 호출한다
5. dry_run signal은 실제 주문 없이 로그만 출력한다
6. 전체 테스트 스위트 green
</success_criteria>

<output>
After completion, create `.planning/phases/03-order-execution/03-01-SUMMARY.md`
</output>
