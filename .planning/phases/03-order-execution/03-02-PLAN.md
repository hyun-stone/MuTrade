---
phase: 03-order-execution
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - mutrade/monitor/scheduler.py
  - mutrade/main.py
  - tests/test_scheduler.py
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04]

must_haves:
  truths:
    - "scheduler 폴링 루프에서 SellSignal 발생 시 dry_run=False이면 executor.execute(signal)이 호출된다"
    - "dry_run=True이면 기존처럼 로그만 출력하고 executor.execute를 호출하지 않는다"
    - "main.py에서 OrderExecutor가 초기화되고 scheduler에 전달된다"
    - "전체 테스트 스위트가 통과한다"
  artifacts:
    - path: "mutrade/monitor/scheduler.py"
      provides: "executor.execute(signal) 호출 경로"
      contains: "executor.execute"
    - path: "mutrade/main.py"
      provides: "OrderExecutor 초기화 및 scheduler 전달"
      contains: "OrderExecutor"
    - path: "tests/test_scheduler.py"
      provides: "executor 통합 테스트"
  key_links:
    - from: "mutrade/monitor/scheduler.py"
      to: "mutrade/executor/order_executor.py"
      via: "executor.execute(signal) 호출"
      pattern: "executor\\.execute\\(sig\\)"
    - from: "mutrade/main.py"
      to: "mutrade/executor/order_executor.py"
      via: "OrderExecutor(kis=kis, dry_run=settings.dry_run)"
      pattern: "OrderExecutor\\("
---

<objective>
OrderExecutor를 scheduler 폴링 루프와 main.py 엔트리포인트에 통합한다. SellSignal 발생 시 dry_run=False이면 실제 매도 주문이 실행되도록 파이프라인을 완성한다.

Purpose: Phase 2에서 로그만 출력하던 SellSignal 처리를 실제 주문 실행으로 전환하여, poll_prices -> engine.tick -> OrderExecutor.execute 전체 파이프라인을 완성한다.
Output: scheduler.py에 executor 통합, main.py에 OrderExecutor 초기화
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-order-execution/03-RESEARCH.md
@.planning/phases/03-order-execution/03-01-SUMMARY.md

<interfaces>
<!-- OrderExecutor: Plan 01에서 생성된 인터페이스 -->
From mutrade/executor/order_executor.py (Plan 01 output):
```python
class OrderExecutor:
    def __init__(self, kis: PyKis, dry_run: bool = False):
        self._kis = kis
        self._dry_run = dry_run
        self._pending: set[str] = set()

    def execute(self, signal: SellSignal) -> None:
        """SellSignal을 받아 매도 주문을 실행한다. dry_run이면 로그만."""
        ...
```

<!-- 기존 scheduler 시그니처 -->
From mutrade/monitor/scheduler.py:
```python
def create_poll_session(kis, config: AppConfig, engine: TrailingStopEngine):
    # 반환하는 run_session() 내에서 signals = engine.tick(prices) 후
    # sig.dry_run이면 DRY-RUN 로그, 아니면 LIVE 로그 출력 (현재 로그만)

def start_scheduler(kis, config: AppConfig, engine: TrailingStopEngine) -> None:
```

<!-- main.py 현재 구조 -->
From mutrade/main.py:
```python
def main() -> None:
    settings = Settings()
    config = load_config()
    kis = create_kis_client(settings)
    store = StateStore(path="state.json")
    engine = TrailingStopEngine(symbols=config.symbols, store=store, dry_run=settings.dry_run)
    start_scheduler(kis, config, engine)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scheduler.py에 OrderExecutor 통합</name>
  <files>mutrade/monitor/scheduler.py, tests/test_scheduler.py</files>
  <read_first>
    - mutrade/monitor/scheduler.py (현재 폴링 루프 — signals 처리 부분 위치 확인)
    - tests/test_scheduler.py (기존 테스트 — engine.tick 호출/SELL SIGNAL 로깅 테스트 패턴)
    - mutrade/executor/order_executor.py (Plan 01에서 만든 OrderExecutor 인터페이스)
  </read_first>
  <action>
    **scheduler.py 수정:**

    1. import 추가: `from mutrade.executor.order_executor import OrderExecutor`

    2. `create_poll_session` 시그니처 변경:
       ```python
       def create_poll_session(kis, config: AppConfig, engine: TrailingStopEngine, executor: OrderExecutor):
       ```
       executor 파라미터 추가 (기존 kis, config, engine 뒤에).

    3. `run_session()` 내 signals 처리 루프 변경 (기존 약 85~92행):
       현재:
       ```python
       for sig in signals:
           logger.warning(
               "[{}] SELL SIGNAL: {} ({}) ...",
               "DRY-RUN" if sig.dry_run else "LIVE",
               ...
           )
       ```
       변경:
       ```python
       for sig in signals:
           logger.warning(
               "[{}] SELL SIGNAL: {} ({}) price={:,.0f} peak={:,.0f} "
               "drop={:.1%} threshold={:.1%}",
               "DRY-RUN" if sig.dry_run else "LIVE",
               sig.code, sig.name, sig.current_price,
               sig.peak_price, sig.drop_pct, sig.threshold,
           )
           if not sig.dry_run:
               executor.execute(sig)
       ```
       dry_run=False인 경우에만 executor.execute(sig) 호출. 로그는 항상 출력 (기존 동작 유지).

    4. `start_scheduler` 시그니처 변경:
       ```python
       def start_scheduler(kis, config: AppConfig, engine: TrailingStopEngine, executor: OrderExecutor) -> None:
       ```
       내부: `create_poll_session(kis, config, engine, executor)` 로 executor 전달.

    **tests/test_scheduler.py 수정:**

    기존 테스트에서 `create_poll_session(kis, config, engine)` 호출 부분에 executor mock 추가.
    executor를 `MagicMock(spec=OrderExecutor)`로 생성하여 4번째 인자로 전달.

    신규 테스트 2개 추가:
    1. `test_live_signal_calls_executor` — dry_run=False signal 발생 시 executor.execute(signal) 호출 확인
    2. `test_dry_run_signal_skips_executor` — dry_run=True signal 발생 시 executor.execute 미호출 확인

    이 테스트들은 기존 `test_sell_signal_logged`와 유사한 구조로, engine.tick이 SellSignal을 반환하도록 mock한 뒤 executor.execute 호출 여부를 검증한다.
  </action>
  <verify>
    <automated>pytest tests/test_scheduler.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `mutrade/monitor/scheduler.py`에 `from mutrade.executor.order_executor import OrderExecutor` 존재
    - `create_poll_session` 시그니처에 `executor: OrderExecutor` 파라미터 포함
    - `start_scheduler` 시그니처에 `executor: OrderExecutor` 파라미터 포함
    - scheduler.py 내 `executor.execute(sig)` 호출이 `if not sig.dry_run:` 조건 안에 위치
    - `tests/test_scheduler.py`에 `test_live_signal_calls_executor` 테스트 존재
    - `tests/test_scheduler.py`에 `test_dry_run_signal_skips_executor` 테스트 존재
    - `pytest tests/test_scheduler.py -x` 전체 통과
  </acceptance_criteria>
  <done>scheduler 폴링 루프에서 LIVE SellSignal이 executor.execute()를 호출하고, DRY-RUN signal은 로그만 출력. 기존 테스트 + 신규 테스트 모두 통과.</done>
</task>

<task type="auto">
  <name>Task 2: main.py에 OrderExecutor 초기화 및 전달</name>
  <files>mutrade/main.py</files>
  <read_first>
    - mutrade/main.py (현재 main() 함수 — engine 초기화 후 start_scheduler 호출 부분)
    - mutrade/executor/order_executor.py (OrderExecutor 생성자 시그니처)
    - mutrade/monitor/scheduler.py (변경된 start_scheduler 시그니처 — executor 파라미터)
  </read_first>
  <action>
    **main.py 수정:**

    1. import 추가 (기존 import 블록에):
       ```python
       from mutrade.executor.order_executor import OrderExecutor
       ```

    2. main() 함수에서 engine 초기화 이후, start_scheduler 호출 이전에 OrderExecutor 생성:
       ```python
       # 매도 주문 실행기 초기화
       executor = OrderExecutor(kis=kis, dry_run=settings.dry_run)
       logger.info(
           "Order executor initialized. dry_run={}",
           settings.dry_run,
       )
       ```

    3. start_scheduler 호출에 executor 전달:
       기존: `start_scheduler(kis, config, engine)`
       변경: `start_scheduler(kis, config, engine, executor)`

    4. 모듈 docstring 업데이트 — 실행 흐름 주석에 "6. OrderExecutor 초기화" 추가.
       기존 "6. APScheduler 시작" → "7. APScheduler 시작"
  </action>
  <verify>
    <automated>pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `mutrade/main.py`에 `from mutrade.executor.order_executor import OrderExecutor` 존재
    - `mutrade/main.py`에 `OrderExecutor(kis=kis, dry_run=settings.dry_run)` 호출 존재
    - `mutrade/main.py`에 `start_scheduler(kis, config, engine, executor)` 호출 존재
    - `pytest tests/ -x -q` 전체 통과
  </acceptance_criteria>
  <done>main.py에서 OrderExecutor가 초기화되고 scheduler에 전달되어, poll_prices -> engine.tick -> executor.execute 전체 파이프라인이 완성됨. 전체 테스트 스위트 통과.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -x -v` — 전체 테스트 스위트 통과
- `grep -n "executor.execute" mutrade/monitor/scheduler.py` — executor 호출 존재
- `grep -n "OrderExecutor" mutrade/main.py` — 초기화 존재
- `grep -n "start_scheduler.*executor" mutrade/main.py` — executor 전달 확인
</verification>

<success_criteria>
1. scheduler 폴링 루프에서 dry_run=False SellSignal 발생 시 executor.execute(signal) 호출됨
2. dry_run=True SellSignal은 로그만 출력 (기존 동작 유지)
3. main.py에서 OrderExecutor가 Settings.dry_run으로 초기화됨
4. 전체 파이프라인: poll_prices -> engine.tick -> executor.execute 연결 완성
5. 전체 테스트 스위트 green (기존 51 + Plan 01 신규 + Plan 02 신규)
</success_criteria>

<output>
After completion, create `.planning/phases/03-order-execution/03-02-SUMMARY.md`
</output>
