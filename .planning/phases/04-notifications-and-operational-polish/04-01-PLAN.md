---
phase: 04-notifications-and-operational-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mutrade/notifier/__init__.py
  - mutrade/notifier/telegram.py
  - mutrade/settings.py
  - pyproject.toml
  - tests/test_telegram_notifier.py
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02

must_haves:
  truths:
    - "TelegramNotifier(token, chat_id).notify(signal, qty)를 호출하면 daemon Thread에서 Bot.send_message가 실행된다"
    - "notify() 내부에서 예외가 발생해도 notify()는 예외를 전파하지 않는다 (D-02)"
    - "TELEGRAM_BOT_TOKEN과 TELEGRAM_CHAT_ID 중 하나만 설정 시 Settings 초기화에서 ValidationError가 발생한다 (D-01)"
    - "두 필드 모두 미설정 시 Settings가 정상 초기화된다 (D-01)"
  artifacts:
    - path: "mutrade/notifier/__init__.py"
      provides: "패키지 초기화 파일 (빈 파일)"
    - path: "mutrade/notifier/telegram.py"
      provides: "TelegramNotifier 클래스"
      exports: ["TelegramNotifier"]
    - path: "mutrade/settings.py"
      provides: "Telegram 선택적 설정 필드 + model_validator"
      contains: "telegram_bot_token, telegram_chat_id"
    - path: "tests/test_telegram_notifier.py"
      provides: "TelegramNotifier 단위 테스트"
  key_links:
    - from: "mutrade/notifier/telegram.py"
      to: "python-telegram-bot Bot.send_message"
      via: "asyncio.run() in daemon Thread"
      pattern: "threading\\.Thread.*daemon=True"
    - from: "mutrade/settings.py"
      to: "model_validator"
      via: "validate_telegram_credentials()"
      pattern: "telegram_bot_token.*telegram_chat_id"
---

<objective>
TelegramNotifier 모듈 신설 및 Settings Telegram 필드 추가.

Purpose: 매도 주문 후 즉각 Telegram 알림을 전송할 수 있는 독립 모듈을 만들고, 설정 레벨에서 자격증명 유효성을 검증한다. 이 플랜은 통합 없이 모듈 자체만 완성한다 — 통합은 04-02에서 수행한다.

Output:
- mutrade/notifier/telegram.py (TelegramNotifier 클래스)
- mutrade/settings.py (Telegram 선택적 필드 + model_validator 확장)
- pyproject.toml (python-telegram-bot==21.11.1 의존성 추가)
- tests/test_telegram_notifier.py (단위 테스트)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-notifications-and-operational-polish/04-CONTEXT.md

<interfaces>
<!-- 이 플랜이 사용하는 기존 인터페이스. 탐색 없이 직접 참조. -->

From mutrade/engine/models.py:
```python
@dataclass(frozen=True)
class SellSignal:
    code: str               # 종목 코드
    name: str               # 종목명
    current_price: float    # 현재가
    peak_price: float       # 고점
    drop_pct: float         # 하락률 (0.10 = 10%)
    threshold: float        # 적용된 임계값
    dry_run: bool           # 드라이런 여부
```

From mutrade/settings.py (기존 model_validator 패턴):
```python
@model_validator(mode="after")
def validate_virtual_credentials(self) -> "Settings":
    """KIS_MOCK=true 일 때 가상 계좌 자격증명이 모두 존재하는지 검증."""
    if self.kis_mock:
        missing = []
        if not self.kis_virtual_id:
            missing.append("KIS_VIRTUAL_ID")
        ...
        if missing:
            raise ValueError(
                f"KIS_MOCK=true requires virtual credentials: {', '.join(missing)}"
            )
```

From pyproject.toml (현재 의존성):
```toml
dependencies = [
    "python-kis==2.1.6",
    "pydantic-settings==2.13.1",
    "python-dotenv==1.2.2",
    "loguru==0.7.3",
    "APScheduler==3.11.2",
    "exchange-calendars==4.13.2",
    "httpx==0.28.1",
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TelegramNotifier 모듈 구현 (테스트 먼저)</name>
  <files>
    mutrade/notifier/__init__.py,
    mutrade/notifier/telegram.py,
    tests/test_telegram_notifier.py
  </files>
  <read_first>
    - mutrade/engine/models.py (SellSignal 필드 확인)
    - mutrade/executor/order_executor.py (로그 패턴 참조)
  </read_first>
  <behavior>
    - test_notify_sends_message: notify(signal, qty=10)를 호출하면 threading.Thread(daemon=True)가 생성된다
    - test_notify_is_nonblocking: notify()는 Thread 시작 후 즉시 반환한다 (0.1초 이내)
    - test_notify_failure_does_not_raise: send_message가 Exception을 발생시켜도 notify()는 예외를 전파하지 않는다
    - test_notify_message_format: 전송된 메시지에 "매도 주문 제출", signal.code, signal.name이 포함된다
    - test_no_token_skips_notify: TelegramNotifier(token=None, chat_id=None).notify(...)는 아무 동작 없이 반환한다
  </behavior>
  <action>
    RED 단계: tests/test_telegram_notifier.py 생성. 위 behavior 5개 케이스를 작성한다.
    mock 전략: unittest.mock.patch("mutrade.notifier.telegram.Bot")으로 Bot 클래스를 모킹한다.
    nonblocking 테스트: time.time()으로 notify() 호출 전후 시간 측정 — Thread.join() 없이 0.1초 이내 반환 검증.

    GREEN 단계: mutrade/notifier/__init__.py 빈 파일 생성 후 mutrade/notifier/telegram.py에 구현:

    ```python
    # mutrade/notifier/telegram.py
    import asyncio
    import threading
    from datetime import datetime
    from zoneinfo import ZoneInfo

    from loguru import logger
    from telegram import Bot

    from mutrade.engine.models import SellSignal

    KST = ZoneInfo("Asia/Seoul")


    class TelegramNotifier:
        """Telegram 알림 전송기.

        BlockingScheduler 단일 스레드를 차단하지 않기 위해
        daemon Thread 내에서 asyncio.run()으로 Bot.send_message를 실행한다.
        token/chat_id가 None이면 모든 notify() 호출을 무시한다 (D-01).
        """

        def __init__(self, token: str | None, chat_id: str | None) -> None:
            self._token = token
            self._chat_id = chat_id

        def notify(self, signal: SellSignal, qty: int) -> None:
            """매도 주문 제출 직후 Telegram 알림을 비동기 전송한다.

            daemon Thread에서 실행 — 메인 스레드 차단 없음 (NOTIF-02, D-02).
            전송 실패 시 logger.error()로 기록 후 무시 (D-02).
            token/chat_id 미설정 시 즉시 반환 (D-01).

            Args:
                signal: 매도 신호 (SellSignal 필드 전체 활용).
                qty: 실제 주문 수량.
            """
            if not self._token or not self._chat_id:
                return

            now_kst = datetime.now(KST).strftime("%Y-%m-%d %H:%M:%S KST")
            # D-04: 정확한 메시지 형식
            text = (
                f"🚨 매도 주문 제출\n"
                f"종목: {signal.name} ({signal.code})\n"
                f"수량: {qty}주 / 현재가: {signal.current_price:,.0f}원\n"
                f"고점: {signal.peak_price:,.0f}원 / 하락률: {signal.drop_pct:.2%}\n"
                f"임계값: {signal.threshold:.1%}\n"
                f"시간: {now_kst}"
            )

            def _send():
                try:
                    bot = Bot(token=self._token)
                    asyncio.run(bot.send_message(chat_id=self._chat_id, text=text))
                except Exception as e:
                    logger.error("Telegram 알림 전송 실패: {}", e)

            thread = threading.Thread(target=_send, daemon=True)
            thread.start()
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_telegram_notifier.py -x -q</automated>
  </verify>
  <done>
    - tests/test_telegram_notifier.py: 5개 테스트 모두 green
    - mutrade/notifier/__init__.py 존재 (빈 파일)
    - mutrade/notifier/telegram.py: TelegramNotifier 클래스 구현 완료
    - grep "daemon=True" mutrade/notifier/telegram.py → 1줄 이상 출력
    - grep "logger.error" mutrade/notifier/telegram.py → "Telegram 알림 전송 실패" 포함
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings에 Telegram 선택적 필드 추가 (테스트 먼저)</name>
  <files>
    mutrade/settings.py,
    pyproject.toml,
    tests/test_settings.py
  </files>
  <read_first>
    - mutrade/settings.py (기존 model_validator 패턴 확인)
    - tests/test_settings.py (기존 테스트 패턴 및 _base_env() 함수 확인)
    - pyproject.toml (현재 dependencies 목록 확인)
  </read_first>
  <behavior>
    - test_telegram_both_absent_ok: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID 모두 미설정 → Settings 정상 초기화, 두 필드 모두 None
    - test_telegram_both_present_ok: 두 필드 모두 설정 → Settings 정상 초기화, 두 필드 모두 해당 값
    - test_telegram_partial_config_raises: TELEGRAM_BOT_TOKEN만 설정(TELEGRAM_CHAT_ID 없음) → ValidationError 발생
    - test_telegram_chat_id_only_raises: TELEGRAM_CHAT_ID만 설정(TELEGRAM_BOT_TOKEN 없음) → ValidationError 발생
  </behavior>
  <action>
    RED 단계: tests/test_settings.py에 아래 4개 테스트를 기존 TestDryRun 클래스 아래에 추가한다.
    기존 테스트 파일 수정 시 기존 테스트를 건드리지 않는다.

    추가할 테스트 클래스:
    ```python
    class TestTelegramSettings:
        def test_telegram_both_absent_ok(self, monkeypatch):
            for k, v in _base_env().items():
                monkeypatch.setenv(k, v)
            monkeypatch.delenv("TELEGRAM_BOT_TOKEN", raising=False)
            monkeypatch.delenv("TELEGRAM_CHAT_ID", raising=False)
            from mutrade.settings import Settings
            s = Settings(_env_file=None)
            assert s.telegram_bot_token is None
            assert s.telegram_chat_id is None

        def test_telegram_both_present_ok(self, monkeypatch):
            for k, v in _base_env().items():
                monkeypatch.setenv(k, v)
            monkeypatch.setenv("TELEGRAM_BOT_TOKEN", "bot123:ABC")
            monkeypatch.setenv("TELEGRAM_CHAT_ID", "-1001234567890")
            from mutrade.settings import Settings
            s = Settings(_env_file=None)
            assert s.telegram_bot_token == "bot123:ABC"
            assert s.telegram_chat_id == "-1001234567890"

        def test_telegram_partial_config_raises(self, monkeypatch):
            for k, v in _base_env().items():
                monkeypatch.setenv(k, v)
            monkeypatch.setenv("TELEGRAM_BOT_TOKEN", "bot123:ABC")
            monkeypatch.delenv("TELEGRAM_CHAT_ID", raising=False)
            from pydantic import ValidationError
            from mutrade.settings import Settings
            with pytest.raises(ValidationError) as exc_info:
                Settings(_env_file=None)
            assert "TELEGRAM_CHAT_ID" in str(exc_info.value)

        def test_telegram_chat_id_only_raises(self, monkeypatch):
            for k, v in _base_env().items():
                monkeypatch.setenv(k, v)
            monkeypatch.delenv("TELEGRAM_BOT_TOKEN", raising=False)
            monkeypatch.setenv("TELEGRAM_CHAT_ID", "-1001234567890")
            from pydantic import ValidationError
            from mutrade.settings import Settings
            with pytest.raises(ValidationError) as exc_info:
                Settings(_env_file=None)
            assert "TELEGRAM_BOT_TOKEN" in str(exc_info.value)
    ```

    GREEN 단계: mutrade/settings.py에 Telegram 필드와 validator 추가.

    기존 필드 블록(dry_run 아래)에 추가:
    ```python
    # Telegram 알림 설정 (선택적 — 둘 다 있거나 둘 다 없어야 함, D-01)
    telegram_bot_token: str | None = Field(None, alias="TELEGRAM_BOT_TOKEN")
    telegram_chat_id: str | None = Field(None, alias="TELEGRAM_CHAT_ID")
    ```

    기존 validate_virtual_credentials 메서드 아래에 새 validator 추가:
    ```python
    @model_validator(mode="after")
    def validate_telegram_credentials(self) -> "Settings":
        """TELEGRAM_BOT_TOKEN과 TELEGRAM_CHAT_ID는 둘 다 있거나 둘 다 없어야 한다 (D-01)."""
        has_token = bool(self.telegram_bot_token)
        has_chat = bool(self.telegram_chat_id)
        if has_token and not has_chat:
            raise ValueError(
                "TELEGRAM_BOT_TOKEN이 설정되었으나 TELEGRAM_CHAT_ID가 없습니다. "
                "둘 다 설정하거나 둘 다 제거하세요."
            )
        if has_chat and not has_token:
            raise ValueError(
                "TELEGRAM_CHAT_ID가 설정되었으나 TELEGRAM_BOT_TOKEN이 없습니다. "
                "둘 다 설정하거나 둘 다 제거하세요."
            )
        return self
    ```

    pyproject.toml: dependencies 목록에 "python-telegram-bot==21.11.1" 추가.
    기존 "httpx==0.28.1" 아래에 삽입:
    ```toml
    "python-telegram-bot==21.11.1",
    ```

    그리고 pip install:
    ```bash
    pip install "python-telegram-bot==21.11.1"
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/test_settings.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    - tests/test_settings.py: TestTelegramSettings 4개 테스트 포함 전체 테스트 green
    - grep "telegram_bot_token" mutrade/settings.py → 필드 선언 확인
    - grep "validate_telegram_credentials" mutrade/settings.py → validator 메서드 확인
    - grep "python-telegram-bot" pyproject.toml → "python-telegram-bot==21.11.1" 확인
    - python -m pytest tests/ -x -q → 기존 14개 + 신규 4개 = 18개 이상 green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → Settings | Telegram Bot Token과 Chat ID가 .env에서 로드됨 — 파일 권한 중요 |
| TelegramNotifier → Telegram Bot API | 토큰이 네트워크 요청에 사용됨 — TLS로 전송되지만 로그에 노출 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | TelegramNotifier._send() | mitigate | logger.error()에서 토큰을 포함한 bot 객체 자체를 로깅하지 않음. 오류 메시지 `e`만 로깅 (e.g., "Telegram 알림 전송 실패: {e}"). Bot(token=) 값은 절대 로그에 출력 금지. |
| T-04-02 | Information Disclosure | mutrade/settings.py | mitigate | telegram_bot_token, telegram_chat_id는 Field(...) 선언에서 repr=False 옵션 추가하거나, Settings 객체를 로깅할 때 해당 필드가 출력되지 않도록 주의. 기존 kis_appkey와 동일 수준의 보호. |
| T-04-03 | Denial of Service | TelegramNotifier.notify() | accept | 알림 전송 실패가 반복될 경우 daemon Thread가 누적될 수 있으나, daemon=True이므로 프로세스 종료 시 함께 소멸. 매도 주문 간격(수 초)을 감안하면 Thread 누적은 실질적 위협 아님. |
| T-04-04 | Denial of Service | _submit_order() → notify() | mitigate | notify()는 Thread.start() 후 즉시 반환 (D-02). 알림 실패가 매도 흐름을 차단하지 않음. try/except Exception으로 _send() 내 모든 예외 포착. |
| T-04-05 | Spoofing | .env 파일 | accept | 개인용 로컬 봇이므로 .env 파일은 단일 사용자 환경에서만 존재. .gitignore로 버전 관리 제외됨 (Phase 1 결정). |
</threat_model>

<verification>
## 전체 플랜 검증

```bash
# 1. TelegramNotifier 단위 테스트
python -m pytest tests/test_telegram_notifier.py -v

# 2. Settings Telegram 필드 테스트
python -m pytest tests/test_settings.py -v

# 3. 전체 테스트 suite (기존 14개 + 신규 테스트 모두 green이어야 함)
python -m pytest tests/ -v

# 4. 구조 확인
ls mutrade/notifier/
grep "TelegramNotifier" mutrade/notifier/telegram.py
grep "telegram_bot_token" mutrade/settings.py
grep "python-telegram-bot" pyproject.toml
```
</verification>

<success_criteria>
1. mutrade/notifier/telegram.py의 TelegramNotifier 클래스가 SellSignal + qty를 받아 D-04 형식의 메시지를 daemon Thread에서 전송한다
2. 전송 실패 시 예외가 전파되지 않고 logger.error()로만 기록된다 (D-02)
3. token/chat_id 중 하나만 설정 시 Settings 초기화에서 ValidationError가 발생한다 (D-01)
4. python -m pytest tests/ -x -q 전체 green (기존 14개 테스트 회귀 없음)
5. Telegram 토큰이 로그에 출력되지 않는다 (T-04-01 mitigated)
</success_criteria>

<output>
완료 후 `.planning/phases/04-notifications-and-operational-polish/04-01-SUMMARY.md` 생성.

포함 내용:
- 생성된 파일 목록 (mutrade/notifier/__init__.py, mutrade/notifier/telegram.py)
- Settings에 추가된 필드 이름
- 테스트 실행 결과 (통과 수)
- 다음 플랜(04-02)을 위한 TelegramNotifier import 경로: `from mutrade.notifier.telegram import TelegramNotifier`
</output>
