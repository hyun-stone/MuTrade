---
phase: 04-notifications-and-operational-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - mutrade/executor/order_executor.py
  - mutrade/monitor/scheduler.py
  - mutrade/main.py
  - tests/test_order_executor.py
  - tests/test_scheduler.py
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "실거래(dry_run=False) 매도 주문 제출 후 logs/mutrade.log에 [TRADE] 마커가 포함된 줄이 기록된다 (NOTIF-03)"
    - "매도 주문 제출 직후 notifier.notify(signal, qty)가 호출된다 (D-03, NOTIF-01)"
    - "scheduler KeyboardInterrupt 처리 블록에서 engine.states 전체를 순회하며 종목코드·고점·warm 상태를 INFO 로그로 출력한다 (NOTIF-04)"
    - "main.py가 TelegramNotifier를 생성하고 OrderExecutor에 주입한다"
  artifacts:
    - path: "mutrade/executor/order_executor.py"
      provides: "notifier 주입 + [TRADE] 로그 + notify() 호출"
      contains: "notifier"
    - path: "mutrade/monitor/scheduler.py"
      provides: "종료 시 engine.states 순회 로깅"
      contains: "engine.states"
    - path: "mutrade/main.py"
      provides: "TelegramNotifier 초기화 및 OrderExecutor 주입"
      contains: "TelegramNotifier"
  key_links:
    - from: "mutrade/main.py"
      to: "mutrade/executor/order_executor.py"
      via: "OrderExecutor(kis=kis, dry_run=..., notifier=notifier)"
      pattern: "TelegramNotifier.*notifier"
    - from: "mutrade/executor/order_executor.py::_submit_order()"
      to: "notifier.notify()"
      via: "logger.warning 직후 호출"
      pattern: "self\\._notifier\\.notify\\(signal"
    - from: "mutrade/monitor/scheduler.py::start_scheduler()"
      to: "engine.states"
      via: "except (KeyboardInterrupt, SystemExit) 블록"
      pattern: "for code.*engine\\.states"
---

<objective>
TelegramNotifier를 OrderExecutor에 통합하고, [TRADE] 로그 마커와 종료 시 상태 로그를 추가한다.

Purpose: 04-01에서 만든 TelegramNotifier를 실제 매도 파이프라인에 연결하여 NOTIF-01~04를 모두 충족시킨다. Phase 4 전체 요구사항의 마지막 통합 단계.

Output:
- mutrade/executor/order_executor.py (notifier 주입, [TRADE] 로그, notify() 호출)
- mutrade/monitor/scheduler.py (종료 시 engine.states 로깅)
- mutrade/main.py (TelegramNotifier 초기화 + OrderExecutor에 주입)
- tests/test_order_executor.py (NOTIF-03 [TRADE] 로그 테스트 추가)
- tests/test_scheduler.py (NOTIF-04 종료 로그 테스트 추가)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-notifications-and-operational-polish/04-CONTEXT.md
@.planning/phases/04-notifications-and-operational-polish/04-01-SUMMARY.md

<interfaces>
<!-- 04-01이 생성한 인터페이스. 탐색 없이 직접 참조. -->

From mutrade/notifier/telegram.py (04-01 생성):
```python
from mutrade.notifier.telegram import TelegramNotifier

class TelegramNotifier:
    def __init__(self, token: str | None, chat_id: str | None) -> None: ...
    def notify(self, signal: SellSignal, qty: int) -> None:
        """daemon Thread에서 Telegram 전송. 실패 시 logger.error + 무시."""
        ...
```

From mutrade/settings.py (04-01 추가):
```python
telegram_bot_token: str | None = Field(None, alias="TELEGRAM_BOT_TOKEN")
telegram_chat_id: str | None = Field(None, alias="TELEGRAM_CHAT_ID")
```

From mutrade/executor/order_executor.py (현재 상태):
```python
class OrderExecutor:
    def __init__(self, kis: PyKis, dry_run: bool = False):
        self._kis = kis
        self._dry_run = dry_run
        self._pending: set[str] = set()

    def _submit_order(self, signal: SellSignal) -> None:
        ...
        order = acc.sell(market="KRX", symbol=signal.code, price=None, qty=qty)
        logger.warning(
            "[LIVE] 매도 주문 제출: {} ({}) qty={} 주문번호={}",
            signal.code, signal.name, qty, order.number,
        )
        # ← 여기에 [TRADE] 로그 + notifier.notify() 삽입 (D-03)
        self._confirm_fill(acc, order, signal.code)
```

From mutrade/monitor/scheduler.py (현재 상태):
```python
def start_scheduler(...):
    ...
    try:
        scheduler.start()
    except (KeyboardInterrupt, SystemExit):
        logger.info("Scheduler stopped by user.")
        # ← 여기에 engine.states 순회 로깅 삽입 (NOTIF-04)

From mutrade/main.py (현재 상태):
```python
executor = OrderExecutor(kis=kis, dry_run=settings.dry_run)
# ← 여기서 TelegramNotifier 초기화 후 executor에 주입
```

From mutrade/engine/models.py:
```python
@dataclass
class SymbolState:
    code: str
    peak_price: float
    warm: bool = False
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OrderExecutor에 notifier 주입 및 [TRADE] 로그 추가 (테스트 먼저)</name>
  <files>
    mutrade/executor/order_executor.py,
    tests/test_order_executor.py
  </files>
  <read_first>
    - mutrade/executor/order_executor.py (현재 __init__ 시그니처, _submit_order 전체 확인)
    - tests/test_order_executor.py (기존 make_kis_mock 및 OrderExecutor(kis) 생성 패턴 확인)
  </read_first>
  <behavior>
    - test_trade_log_emitted: 실거래(dry_run=False) execute() 후 loguru caplog에 "[TRADE]"가 포함된 로그 줄이 존재한다
    - test_notifier_called_after_sell: notifier mock의 notify(signal, qty)가 acc.sell() 이후에 정확히 1회 호출된다
    - test_notifier_not_called_in_dry_run: dry_run=True signal에서는 notifier.notify()가 호출되지 않는다
    - test_notifier_none_works: notifier=None으로 OrderExecutor 초기화 시 기존 동작 그대로 (sell 호출, 예외 없음)
  </behavior>
  <action>
    RED 단계: tests/test_order_executor.py에 TestNotifierIntegration 클래스 추가.

    loguru caplog 패턴 (loguru는 pytest caplog를 직접 지원하지 않으므로 propagate 패턴 사용):
    ```python
    import logging
    from loguru import logger

    @pytest.fixture
    def caplog_loguru(caplog):
        """loguru 로그를 pytest caplog에 전달하는 픽스처."""
        handler_id = logger.add(
            lambda msg: caplog.handler.emit(
                logging.LogRecord("loguru", logging.INFO, "", 0, msg, [], None)
            )
        )
        yield caplog
        logger.remove(handler_id)
    ```

    추가 테스트 클래스:
    ```python
    class TestNotifierIntegration:
        @patch("mutrade.executor.order_executor.time.sleep")
        def test_trade_log_emitted(self, mock_sleep, caplog):
            """실거래 execute() 후 [TRADE] 마커가 로그에 출력된다."""
            kis, acc, order = make_kis_mock(orderable=10)
            notifier = MagicMock()
            executor = OrderExecutor(kis, notifier=notifier)
            import logging
            with caplog.at_level(logging.INFO, logger="mutrade.executor.order_executor"):
                # loguru를 propagate 활성화로 캡처
                with patch("mutrade.executor.order_executor.logger") as mock_logger:
                    info_calls = []
                    mock_logger.info.side_effect = lambda msg, *a, **kw: info_calls.append(msg)
                    mock_logger.warning.side_effect = lambda msg, *a, **kw: None
                    mock_logger.error.side_effect = lambda msg, *a, **kw: None
                    executor.execute(make_signal())
            assert any("[TRADE]" in c for c in info_calls), f"[TRADE] not found in: {info_calls}"

        @patch("mutrade.executor.order_executor.time.sleep")
        def test_notifier_called_after_sell(self, mock_sleep):
            """notifier.notify(signal, qty)가 acc.sell() 직후 1회 호출된다."""
            kis, acc, order = make_kis_mock(orderable=10)
            notifier = MagicMock()
            executor = OrderExecutor(kis, notifier=notifier)
            signal = make_signal()
            executor.execute(signal)
            notifier.notify.assert_called_once_with(signal, 10)

        @patch("mutrade.executor.order_executor.time.sleep")
        def test_notifier_not_called_in_dry_run(self, mock_sleep):
            """dry_run=True signal에서는 notifier.notify()가 호출되지 않는다."""
            kis, acc, order = make_kis_mock()
            notifier = MagicMock()
            executor = OrderExecutor(kis, notifier=notifier)
            executor.execute(make_signal(dry_run=True))
            notifier.notify.assert_not_called()

        @patch("mutrade.executor.order_executor.time.sleep")
        def test_notifier_none_works(self, mock_sleep):
            """notifier=None(기본값)으로 OrderExecutor 초기화 시 sell 정상 호출."""
            kis, acc, order = make_kis_mock(orderable=10)
            executor = OrderExecutor(kis)  # notifier 없음
            executor.execute(make_signal())
            acc.sell.assert_called_once()
    ```

    GREEN 단계: mutrade/executor/order_executor.py 수정.

    1. import 추가 (파일 최상단):
       ```python
       from __future__ import annotations
       from typing import TYPE_CHECKING
       if TYPE_CHECKING:
           from mutrade.notifier.telegram import TelegramNotifier
       ```

    2. __init__ 시그니처 변경:
       ```python
       def __init__(
           self,
           kis: PyKis,
           dry_run: bool = False,
           notifier: "TelegramNotifier | None" = None,
       ):
           self._kis = kis
           self._dry_run = dry_run
           self._pending: set[str] = set()
           self._notifier = notifier
       ```

    3. _submit_order()에서 logger.warning("[LIVE] 매도 주문 제출: ...") 직후에 아래 두 줄 삽입:
       ```python
       # NOTIF-03: [TRADE] 마커로 거래 이력 기록 — grep "[TRADE]"로 추출 가능
       logger.info(
           "[TRADE] 매도 주문 제출: {} ({}) qty={} current_price={:,.0f} "
           "peak={:,.0f} drop={:.2%} threshold={:.1%} order={}",
           signal.code, signal.name, qty,
           signal.current_price, signal.peak_price,
           signal.drop_pct, signal.threshold,
           order.number,
       )
       # NOTIF-01: Telegram 알림 전송 (D-03: acc.sell() 직후, _confirm_fill() 전)
       if self._notifier is not None:
           self._notifier.notify(signal, qty)
       ```

    주의: notifier.notify() 호출에서 예외가 발생하더라도 TelegramNotifier 내부에서 이미 포착하므로
    order_executor.py에서는 별도 try/except 불필요.
  </action>
  <verify>
    <automated>python -m pytest tests/test_order_executor.py -x -q</automated>
  </verify>
  <done>
    - tests/test_order_executor.py: TestNotifierIntegration 4개 테스트 포함 전체 green
    - grep "self._notifier" mutrade/executor/order_executor.py → __init__ + _submit_order 2곳 확인
    - grep "\[TRADE\]" mutrade/executor/order_executor.py → 1줄 이상
    - grep "notifier" mutrade/executor/order_executor.py → __init__ 시그니처 포함 확인
    - 기존 10개 OrderExecutor 테스트 회귀 없음
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 종료 로그 및 main.py 통합 (테스트 먼저)</name>
  <files>
    mutrade/monitor/scheduler.py,
    mutrade/main.py,
    tests/test_scheduler.py
  </files>
  <read_first>
    - mutrade/monitor/scheduler.py (start_scheduler의 except 블록 L149-152 확인)
    - mutrade/main.py (OrderExecutor 초기화 L83-87 확인, import 목록 확인)
    - tests/test_scheduler.py (make_engine_mock 패턴 확인 — engine.states 딕셔너리)
  </read_first>
  <behavior>
    - test_shutdown_logs_state: start_scheduler()가 KeyboardInterrupt로 종료될 때 engine.states의 각 SymbolState에 대해 logger.info가 code, peak_price, warm을 포함하여 호출된다
  </behavior>
  <action>
    RED 단계: tests/test_scheduler.py에 TestShutdownLog 클래스 추가.

    ```python
    class TestShutdownLog:
        def test_shutdown_logs_state(self):
            """KeyboardInterrupt 종료 시 engine.states 전체를 순회하며 로그를 출력한다."""
            from mutrade.monitor.scheduler import start_scheduler
            from mutrade.engine.models import SymbolState

            kis = MagicMock()
            config = make_config()
            engine = make_engine_mock()
            executor = make_executor_mock()

            # engine.states에 2개 종목 설정
            engine.states = {
                "005930": SymbolState(code="005930", peak_price=80000.0, warm=True),
                "000660": SymbolState(code="000660", peak_price=150000.0, warm=False),
            }

            info_calls = []

            with patch("mutrade.monitor.scheduler.BlockingScheduler") as mock_sched_cls, \
                 patch("mutrade.monitor.scheduler.logger") as mock_logger:
                mock_sched = MagicMock()
                mock_sched_cls.return_value = mock_sched
                # scheduler.start()가 KeyboardInterrupt를 발생시키도록 설정
                mock_sched.start.side_effect = KeyboardInterrupt()
                mock_logger.info.side_effect = lambda msg, *a, **kw: info_calls.append(str(msg) + str(a))

                start_scheduler(kis, config, engine, executor)

            # engine.states의 2개 종목이 모두 로깅되어야 함
            combined = " ".join(info_calls)
            assert "005930" in combined, f"005930 not in shutdown logs: {info_calls}"
            assert "000660" in combined, f"000660 not in shutdown logs: {info_calls}"
    ```

    GREEN 단계 (1): mutrade/monitor/scheduler.py 수정.

    start_scheduler()의 except 블록을 다음으로 교체:
    ```python
    except (KeyboardInterrupt, SystemExit):
        logger.info("Scheduler stopped by user.")
        # NOTIF-04: 종료 시 모니터링 종목 및 고점 상태 로깅
        logger.info("=== 종료 시 모니터링 상태 ===")
        for code, state in engine.states.items():
            logger.info(
                "  {} peak={:,.0f} warm={}",
                code, state.peak_price, state.warm,
            )
        logger.info("MuTrade 봇 종료 완료.")
    ```

    start_scheduler 시그니처는 변경하지 않는다 — engine 파라미터는 이미 존재함.

    GREEN 단계 (2): mutrade/main.py 수정.

    기존 OrderExecutor 초기화 코드를 찾아 TelegramNotifier 생성 및 주입 코드로 교체:

    1. import 추가 (파일 상단 import 블록에):
       ```python
       from mutrade.notifier.telegram import TelegramNotifier
       ```

    2. OrderExecutor 초기화 블록 교체:
       ```python
       # Telegram 알림기 초기화 (D-01: 둘 다 없으면 None 반환 — 알림 없이 정상 실행)
       notifier = TelegramNotifier(
           token=settings.telegram_bot_token,
           chat_id=settings.telegram_chat_id,
       )
       if settings.telegram_bot_token:
           logger.info("Telegram 알림 활성화. chat_id={}", settings.telegram_chat_id)
       else:
           logger.info("Telegram 알림 비활성화 (TELEGRAM_BOT_TOKEN 미설정).")

       # 매도 주문 실행기 초기화
       executor = OrderExecutor(kis=kis, dry_run=settings.dry_run, notifier=notifier)
       logger.info(
           "Order executor initialized. dry_run={}",
           settings.dry_run,
       )
       ```

    주의: main.py의 TelegramNotifier 초기화에서 token 값을 로그에 출력하지 않는다 (T-04-01).
    chat_id만 출력하며, 이는 채널 식별자로 보안 민감도가 낮다.
  </action>
  <verify>
    <automated>python -m pytest tests/test_scheduler.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    - tests/test_scheduler.py: TestShutdownLog 1개 테스트 포함 전체 green
    - grep "engine.states" mutrade/monitor/scheduler.py → except 블록 내 순회 코드 확인
    - grep "TelegramNotifier" mutrade/main.py → import + 초기화 2곳 확인
    - grep "notifier=notifier" mutrade/main.py → OrderExecutor 생성 인자 확인
    - python -m pytest tests/ -x -q → 전체 green (기존 14개 + 신규 테스트 모두 통과)
    - grep "TELEGRAM_BOT_TOKEN\|telegram_bot_token" mutrade/main.py → logger.info 호출에 token 값 미출력 확인
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OrderExecutor → TelegramNotifier | SellSignal 데이터(종목코드, 가격)가 알림 모듈로 전달됨 |
| scheduler except 블록 → engine.states | 고점 가격 데이터가 로그에 기록됨 |
| main.py → Settings | Telegram 토큰이 Settings 객체에서 notifier로 전달됨 — 중간 경유 시 노출 주의 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | mutrade/main.py TelegramNotifier 초기화 | mitigate | logger.info에서 settings.telegram_bot_token 값을 출력하지 않음. chat_id만 출력 (채널 식별자, 저위험). token은 절대 로그에 출력 금지. |
| T-04-07 | Denial of Service | OrderExecutor._submit_order() | mitigate | notifier.notify() 예외는 TelegramNotifier 내부에서 포착됨. order_executor.py에서 추가 try/except 불필요. 매도 흐름 차단 위험 없음 (D-02). |
| T-04-08 | Information Disclosure | [TRADE] 로그 → logs/mutrade.log | accept | [TRADE] 로그에 종목코드·가격·수량만 기록. API 키, 토큰 등 자격증명 미포함. 로그 파일은 로컬 환경에서만 접근. |
| T-04-09 | Tampering | engine.states 종료 로그 | accept | 로그는 정보 전용, 상태 변경 없음. SymbolState.peak_price는 읽기 전용 순회. |
| T-04-10 | Information Disclosure | OrderExecutor 생성자 notifier 파라미터 | accept | notifier 객체 자체는 자격증명 미포함 (token은 TelegramNotifier 내부). 기본값 None으로 미설정 시 안전. |
</threat_model>

<verification>
## 전체 플랜 검증

```bash
# 1. 전체 테스트 suite
python -m pytest tests/ -v

# 2. [TRADE] 마커 삽입 확인
grep "\[TRADE\]" mutrade/executor/order_executor.py

# 3. notifier 통합 확인
grep "_notifier" mutrade/executor/order_executor.py

# 4. 종료 로그 삽입 확인
grep "engine.states" mutrade/monitor/scheduler.py

# 5. main.py 통합 확인
grep "TelegramNotifier" mutrade/main.py
grep "notifier=notifier" mutrade/main.py

# 6. 토큰이 로그에 출력되지 않음 확인
grep "telegram_bot_token" mutrade/main.py | grep "logger"
# → 출력 없어야 함 (logger에 token 전달 금지)
```
</verification>

<success_criteria>
1. 실거래 매도 execute() 후 logs/mutrade.log에 "[TRADE]" 마커가 포함된 로그 줄이 기록된다 (NOTIF-03)
2. notifier.notify(signal, qty)가 acc.sell() 직후, _confirm_fill() 전에 호출된다 (D-03, NOTIF-01)
3. KeyboardInterrupt 종료 시 engine.states의 모든 종목 코드, 고점, warm 상태가 로그에 출력된다 (NOTIF-04)
4. main.py에서 TelegramNotifier가 초기화되어 OrderExecutor에 주입된다
5. python -m pytest tests/ -x -q 전체 green — Phase 1~3 기존 테스트 회귀 없음
6. Telegram Bot Token이 어떤 로그에도 평문으로 출력되지 않는다 (T-04-06 mitigated)
</success_criteria>

<output>
완료 후 `.planning/phases/04-notifications-and-operational-polish/04-02-SUMMARY.md` 생성.

포함 내용:
- 수정된 파일 목록 및 변경 요약
- 최종 테스트 통과 수 (기존 14개 + 신규 X개)
- Phase 4 완료 확인 — NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04 모두 충족
- 수동 검증 필요 사항: 실제 Telegram 봇 토큰으로 end-to-end 알림 수신 확인
</output>
