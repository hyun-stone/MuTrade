---
phase: 05-process-architecture-migration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mutrade/admin/__init__.py
  - mutrade/admin/hub.py
  - mutrade/admin/app.py
  - pyproject.toml
  - tests/test_hub.py
autonomous: true
requirements:
  - INFRA-P5  # Phase 6~8 전제 조건 인프라
must_haves:
  truths:
    - "BotStateHub는 봇 스레드에서 push_snapshot()을 호출해도 RuntimeError가 발생하지 않는다"
    - "BotStateHub는 asyncio 루프에서 get_snapshot()을 호출해도 RuntimeError가 발생하지 않는다"
    - "BotStateHub의 stop_event는 FastAPI 엔드포인트(asyncio 루프)에서 set하고 봇 스레드에서 is_set()으로 읽어도 안전하다"
    - "FastAPI 앱의 GET /health 엔드포인트가 200을 반환한다"
    - "uvicorn 서버가 포트 8000에서 시작 가능하다"
  artifacts:
    - path: "mutrade/admin/hub.py"
      provides: "BotStateHub — threading.RLock + asyncio.Queue + threading.Event"
      exports:
        - BotStateHub
    - path: "mutrade/admin/app.py"
      provides: "FastAPI 앱 팩토리 + lifespan + /health"
      exports:
        - create_app
    - path: "tests/test_hub.py"
      provides: "BotStateHub 스레드 안전성 테스트"
  key_links:
    - from: "APScheduler 스레드 (push_snapshot)"
      to: "asyncio.Queue"
      via: "loop.call_soon_threadsafe(queue.put_nowait, snapshot)"
      pattern: "call_soon_threadsafe"
    - from: "FastAPI lifespan"
      to: "BotStateHub"
      via: "hub.attach_loop(asyncio.get_running_loop())"
      pattern: "attach_loop"
---

<objective>
BotStateHub를 구현하고, FastAPI 최소 앱을 생성하여 Phase 5의 스레드-asyncio 브릿지 인프라를 완성한다.

Purpose: APScheduler 스레드(봇 폴링)와 uvicorn asyncio 이벤트 루프 사이에서 상태를 안전하게 공유하기 위한 핵심 컴포넌트. Phase 6~8 모든 실시간 기능의 전제 조건.

Output:
- mutrade/admin/hub.py — BotStateHub (RLock + asyncio.Queue + threading.Event)
- mutrade/admin/app.py — FastAPI 앱 팩토리 (lifespan + /health)
- tests/test_hub.py — 스레드 안전성 TDD 테스트
- pyproject.toml — fastapi, uvicorn[standard], jinja2, python-multipart 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<interfaces>
<!-- 기존 코드베이스에서 추출한 핵심 타입 -->

From mutrade/engine/models.py (추정 — executor가 사용하는 SymbolState):
```python
# engine.states 프로퍼티가 반환하는 dict 구조 (read-only copy)
# key: str (종목코드), value: SymbolState dataclass
# SymbolState 필드: code: str, peak_price: float, warm: bool
```

From mutrade/monitor/scheduler.py (현재):
```python
# start_scheduler(kis, config, engine, executor) -> None  (현재 블로킹)
# create_poll_session(kis, config, engine, executor) -> Callable
```

BotStateHub가 제공할 인터페이스 (이 Plan에서 정의):
```python
class BotStateHub:
    def attach_loop(self, loop: asyncio.AbstractEventLoop) -> None: ...
    def push_snapshot(self, states: dict) -> None: ...   # APScheduler 스레드에서 호출
    def get_snapshot(self) -> dict: ...                  # FastAPI asyncio 루프에서 호출
    async def wait_for_change(self) -> dict: ...         # WebSocket 브로드캐스트 태스크에서 await
    def request_stop(self) -> None: ...
    def clear_stop(self) -> None: ...
    def is_stop_requested(self) -> bool: ...
    def set_running(self, running: bool) -> None: ...
    def is_running(self) -> bool: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BotStateHub 구현 (TDD)</name>
  <files>mutrade/admin/__init__.py, mutrade/admin/hub.py, tests/test_hub.py</files>
  <behavior>
    - Test 1: push_snapshot()을 봇 스레드에서 호출 시 RuntimeError 없이 완료 (attach_loop 전)
    - Test 2: attach_loop() 후 push_snapshot() 호출 시 asyncio.Queue에 스냅샷이 전달된다 (call_soon_threadsafe 검증)
    - Test 3: get_snapshot()은 push_snapshot()이 마지막으로 전달한 dict의 복사본을 반환한다
    - Test 4: request_stop() 호출 후 is_stop_requested()가 True를 반환한다
    - Test 5: clear_stop() 호출 후 is_stop_requested()가 False를 반환한다
    - Test 6: set_running(True) 후 is_running()이 True를 반환한다
    - Test 7: push_snapshot()과 get_snapshot()이 서로 다른 스레드에서 동시에 호출되어도 데이터 경합(race condition) 없이 완료된다 (threading.Thread 2개로 병렬 호출)
    - Test 8: attach_loop()를 호출하지 않은 상태에서 push_snapshot()을 호출해도 AttributeError/RuntimeError가 발생하지 않는다 (loop=None 방어 처리)
  </behavior>
  <action>
    1. mutrade/admin/__init__.py 생성 (빈 파일 — 패키지 선언용)

    2. mutrade/admin/hub.py 생성:

    ```python
    """
    mutrade/admin/hub.py

    BotStateHub — APScheduler 스레드와 FastAPI asyncio 이벤트 루프 사이의 상태 브릿지.

    설계 원칙:
    - push_snapshot(): APScheduler 스레드에서 호출. loop.call_soon_threadsafe()로 asyncio.Queue에 삽입.
    - get_snapshot(): asyncio 루프 또는 어느 스레드에서도 호출 가능. RLock으로 보호.
    - stop_event: threading.Event — 스레드 안전 플래그. FastAPI → APScheduler 스레드 방향 제어.
    """
    import asyncio
    import threading
    from typing import Optional

    class BotStateHub:
        def __init__(self) -> None:
            self._lock = threading.RLock()
            self._snapshot: dict = {}
            self._loop: Optional[asyncio.AbstractEventLoop] = None
            self._change_queue: Optional[asyncio.Queue] = None
            self._stop_event = threading.Event()
            self._is_running: bool = False

        def attach_loop(self, loop: asyncio.AbstractEventLoop) -> None:
            """FastAPI lifespan startup에서 호출. asyncio 이벤트 루프와 Queue를 연결."""
            with self._lock:
                self._loop = loop
                self._change_queue = asyncio.Queue(maxsize=1)

        def push_snapshot(self, states: dict) -> None:
            """
            APScheduler 스레드에서 호출. engine.states 딕셔너리를 직렬화하여 저장.
            attach_loop()가 호출된 경우 asyncio.Queue에도 push.
            states: engine.states (SymbolState dict 또는 이미 직렬화된 dict)
            """
            # SymbolState dataclass → plain dict 직렬화 (Phase 6에서 필드 추가 가능)
            serialized: dict = {}
            for code, s in states.items():
                if hasattr(s, '__dataclass_fields__') or hasattr(s, 'peak_price'):
                    serialized[code] = {
                        "code": getattr(s, 'code', code),
                        "peak_price": getattr(s, 'peak_price', 0.0),
                        "warm": getattr(s, 'warm', False),
                    }
                else:
                    serialized[code] = s  # 이미 dict인 경우

            with self._lock:
                self._snapshot = serialized

            # asyncio.Queue에 삽입 (loop가 연결된 경우만)
            # call_soon_threadsafe: 스레드 안전 — asyncio 이벤트 루프 스레드에서 실행됨
            if self._loop is not None and self._change_queue is not None:
                try:
                    self._loop.call_soon_threadsafe(
                        self._change_queue.put_nowait, dict(serialized)
                    )
                except RuntimeError:
                    # 루프가 닫힌 경우 (shutdown 중) — 무시
                    pass

        def get_snapshot(self) -> dict:
            """asyncio 루프에서 호출. 최신 스냅샷의 복사본 반환."""
            with self._lock:
                return dict(self._snapshot)

        async def wait_for_change(self) -> dict:
            """
            WebSocket 브로드캐스트 태스크에서 await.
            새 스냅샷이 push될 때까지 대기. asyncio 이벤트 루프에서만 호출 가능.
            attach_loop() 전에 호출되면 RuntimeError를 raise (개발 오용 방지).
            """
            if self._change_queue is None:
                raise RuntimeError(
                    "BotStateHub.attach_loop()가 먼저 호출되어야 합니다."
                )
            return await self._change_queue.get()

        def request_stop(self) -> None:
            """FastAPI 엔드포인트에서 봇 폴링 중단 요청. threading.Event — 스레드 안전."""
            self._stop_event.set()

        def clear_stop(self) -> None:
            """중단 플래그 초기화. 봇 재시작 전 호출."""
            self._stop_event.clear()

        def is_stop_requested(self) -> bool:
            """APScheduler 스레드의 poll 루프에서 중단 요청 여부 확인."""
            return self._stop_event.is_set()

        def set_running(self, running: bool) -> None:
            """APScheduler 스레드가 세션 시작/종료 시 호출."""
            with self._lock:
                self._is_running = running

        def is_running(self) -> bool:
            """봇 실행 상태 조회. FastAPI 엔드포인트에서 호출."""
            with self._lock:
                return self._is_running
    ```

    3. tests/test_hub.py 생성 (TDD RED 단계 — 위 구현 전에 작성):

    테스트 커버리지:
    - push_snapshot() loop 없이 호출 → 예외 없음
    - attach_loop() + push_snapshot() → call_soon_threadsafe 검증 (unittest.mock.patch)
    - get_snapshot() → 마지막 push 값 반환
    - request_stop/clear_stop/is_stop_requested 시퀀스
    - set_running/is_running
    - 멀티스레드 동시 push + get (threading.Thread 2개, 50회 반복)
    - attach_loop 없이 push_snapshot → 예외 없음 (방어 처리)

    테스트는 asyncio 없이 실행 가능하도록 작성 (asyncio.Queue mock 사용).
    wait_for_change() 테스트는 pytest-asyncio 없이 asyncio.run()으로 작성.
  </action>
  <verify>
    <automated>cd /Users/sean/Study/MuTrade/MuTrade && python -m pytest tests/test_hub.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/test_hub.py의 8개 테스트 모두 PASS
    - mutrade/admin/hub.py가 존재하고 BotStateHub 클래스를 export
    - mutrade/admin/__init__.py 존재
  </done>
</task>

<task type="auto">
  <name>Task 2: FastAPI 최소 앱 + pyproject.toml 의존성 추가</name>
  <files>mutrade/admin/app.py, pyproject.toml</files>
  <action>
    1. pyproject.toml dependencies에 추가:
       - "fastapi==0.135.3"
       - "uvicorn[standard]==0.44.0"
       - "jinja2==3.1.6"
       - "python-multipart==0.0.26"

    2. mutrade/admin/app.py 생성:

    ```python
    """
    mutrade/admin/app.py

    FastAPI 앱 팩토리.

    lifespan:
    - startup: asyncio 이벤트 루프를 BotStateHub에 연결 (attach_loop)
    - shutdown: hub 상태 정리

    엔드포인트:
    - GET /health — 서버 생존 확인
    """
    import asyncio
    from contextlib import asynccontextmanager
    from typing import Any

    from fastapi import FastAPI
    from loguru import logger

    from mutrade.admin.hub import BotStateHub


    def create_app(hub: BotStateHub, **kwargs: Any) -> FastAPI:
        """
        FastAPI 앱 인스턴스를 생성한다.

        Args:
            hub: BotStateHub 인스턴스 (봇 상태 브릿지)
            **kwargs: 추후 engine, scheduler 등 의존성 주입용 (Phase 6~7에서 사용)

        Returns:
            FastAPI 앱 인스턴스
        """

        @asynccontextmanager
        async def lifespan(app: FastAPI):
            # startup: 현재 asyncio 이벤트 루프를 hub에 연결 (get_running_loop — Python 3.10+ 권장)
            loop = asyncio.get_running_loop()
            hub.attach_loop(loop)
            app.state.hub = hub
            logger.info("Admin server started. BotStateHub loop attached.")
            yield
            # shutdown: 봇 폴링 중단 요청 + BackgroundScheduler 종료
            hub.request_stop()
            scheduler = kwargs.get("scheduler")
            if scheduler is not None:
                scheduler.shutdown(wait=False)
            logger.info("Admin server shutting down.")

        app = FastAPI(
            title="MuTrade Admin",
            description="트레일링 스탑 봇 관리 대시보드",
            version="1.1.0",
            lifespan=lifespan,
        )

        @app.get("/health")
        async def health() -> dict:
            """서버 생존 확인 및 봇 실행 상태 반환."""
            return {
                "status": "ok",
                "bot_running": hub.is_running(),
                "stop_requested": hub.is_stop_requested(),
            }

        return app
    ```

    3. pip install 실행 (새 의존성):
       ```bash
       cd /Users/sean/Study/MuTrade/MuTrade && pip install -e ".[dev]"
       ```
  </action>
  <verify>
    <automated>cd /Users/sean/Study/MuTrade/MuTrade && python -c "from mutrade.admin.app import create_app; from mutrade.admin.hub import BotStateHub; app = create_app(BotStateHub()); print('OK')" && python -m pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - mutrade/admin/app.py import 성공, create_app() 호출 가능
    - pyproject.toml에 fastapi, uvicorn[standard], jinja2, python-multipart 추가됨
    - 기존 86개 테스트 포함 전체 테스트 PASS
    - python -c "import fastapi, uvicorn" 오류 없음
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APScheduler 스레드 → asyncio 루프 | 봇 폴링 스레드가 asyncio 이벤트 루프의 Queue에 데이터 삽입 |
| HTTP 클라이언트 → FastAPI | 외부(로컬호스트) HTTP 요청이 /health 엔드포인트에 도달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | BotStateHub._snapshot | mitigate | get_snapshot()이 dict() 복사본 반환 — 호출자가 반환값 수정해도 내부 상태 변경 불가 |
| T-05-02 | Denial of Service | asyncio.Queue (unbounded) | accept | 봇 폴링 주기가 3~5초, 개인용 단일 연결 — 큐 폭발 위험 없음. Phase 6에서 maxsize 설정 고려 |
| T-05-03 | Elevation of Privilege | /health 엔드포인트 | accept | 127.0.0.1 로컬 바인딩. 개인용 봇 — 인증 불필요. 외부 노출 시 v1.2에서 Basic Auth 추가 |
| T-05-04 | Information Disclosure | /health 응답 (bot_running, stop_requested) | accept | 로컬 바인딩, 운영자 본인만 접근. 민감 정보(API 키, 토큰) 미포함 |
| T-05-05 | Denial of Service | call_soon_threadsafe 루프 종료 후 호출 | mitigate | hub.push_snapshot()에서 RuntimeError catch 후 pass — 서버 shutdown 중 예외 억제 |
</threat_model>

<verification>
전체 Phase 5 Plan 01 완료 체크:

```bash
# 1. 새 패키지 import 확인
python -c "import fastapi, uvicorn, jinja2; print('deps OK')"

# 2. admin 패키지 구조 확인
python -c "from mutrade.admin.hub import BotStateHub; from mutrade.admin.app import create_app; print('admin OK')"

# 3. BotStateHub 기본 동작
python -c "
from mutrade.admin.hub import BotStateHub
hub = BotStateHub()
hub.push_snapshot({'005930': type('S', (), {'code':'005930','peak_price':75000.0,'warm':True})()})
snap = hub.get_snapshot()
assert '005930' in snap
hub.request_stop()
assert hub.is_stop_requested()
hub.clear_stop()
assert not hub.is_stop_requested()
print('BotStateHub OK')
"

# 4. 전체 테스트 통과 (기존 86개 + 신규 test_hub.py)
python -m pytest tests/ -v --tb=short 2>&1 | tail -20
```
</verification>

<success_criteria>
- [ ] mutrade/admin/__init__.py, hub.py, app.py 파일 존재
- [ ] tests/test_hub.py의 8개 테스트 모두 PASS
- [ ] 기존 86개 테스트 전부 PASS (regression 없음)
- [ ] pyproject.toml에 fastapi==0.135.3, uvicorn[standard]==0.44.0, jinja2==3.1.6, python-multipart==0.0.26 추가
- [ ] BotStateHub: push_snapshot() loop 없이도 예외 없음
- [ ] BotStateHub: get_snapshot()이 복사본 반환 (참조 공유 없음)
- [ ] create_app(hub) 호출 시 FastAPI 인스턴스 반환
- [ ] GET /health 엔드포인트가 {"status": "ok", ...} 반환
</success_criteria>

<output>
완료 후 `.planning/phases/05-process-architecture-migration/05-01-SUMMARY.md` 생성.
</output>
