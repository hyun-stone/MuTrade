---
phase: 06-모니터링-대시보드
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mutrade/admin/hub.py
  - mutrade/executor/order_executor.py
  - mutrade/monitor/scheduler.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "asyncio.QueueFull 예외가 발생하지 않는다 — 큐가 가득 찼을 때 기존 항목을 버리고 새 항목 삽입"
    - "hub.get_snapshot() 반환값에 current_price, drop_pct, sell_pending 필드가 포함된다"
    - "executor.pending_codes()가 현재 SELL_PENDING 종목의 frozenset을 반환한다"
    - "scheduler.py의 push_snapshot() 호출이 prices와 pending_codes를 함께 전달한다"
  artifacts:
    - path: "mutrade/admin/hub.py"
      provides: "드롭-앤-리플레이스 QueueFull 수정 + push_snapshot(states, prices, pending_codes) 시그니처"
      contains: "_put_snapshot"
    - path: "mutrade/executor/order_executor.py"
      provides: "pending_codes() 공개 메서드"
      exports: ["pending_codes"]
    - path: "mutrade/monitor/scheduler.py"
      provides: "executor.pending_codes() + prices를 push_snapshot()에 전달"
      contains: "executor.pending_codes()"
  key_links:
    - from: "mutrade/monitor/scheduler.py"
      to: "mutrade/admin/hub.py"
      via: "hub.push_snapshot(engine.states, prices, executor.pending_codes())"
      pattern: "push_snapshot.*pending_codes"
    - from: "mutrade/admin/hub.py"
      to: "asyncio.Queue"
      via: "_put_snapshot() — call_soon_threadsafe 경유"
      pattern: "_put_snapshot"
---

<objective>
인프라 버그 수정 및 스냅샷 데이터 확장.

Purpose: Wave 2(WebSocket 엔드포인트)와 Wave 3(대시보드 UI)의 전제조건. asyncio.QueueFull 버그를 수정하지 않으면 WebSocket 브로드캐스트가 불안정하고, push_snapshot() 시그니처를 확장하지 않으면 UI에 표시할 데이터가 없다.

Output:
- hub.py: QueueFull 버그 수정 + push_snapshot(states, prices, pending_codes) 시그니처 확장
- order_executor.py: pending_codes() 공개 메서드 추가
- scheduler.py: push_snapshot() 호출 갱신
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-모니터링-대시보드/06-CONTEXT.md
@.planning/phases/06-모니터링-대시보드/06-RESEARCH.md

<interfaces>
<!-- 수정 대상 파일의 현재 핵심 인터페이스 -->

mutrade/admin/hub.py:
```python
class BotStateHub:
    def attach_loop(self, loop: asyncio.AbstractEventLoop) -> None: ...
    def push_snapshot(self, states: dict) -> None: ...   # 수정 대상
    def get_snapshot(self) -> dict: ...
    async def wait_for_change(self) -> dict: ...
    def set_running(self, running: bool) -> None: ...
    def is_running(self) -> bool: ...
    def is_stop_requested(self) -> bool: ...
```

mutrade/executor/order_executor.py:
```python
class OrderExecutor:
    _pending: set[str]   # 현재 비공개
    def execute(self, signal: SellSignal) -> None: ...
    # pending_codes() 메서드 없음 — 추가 필요
```

mutrade/monitor/scheduler.py — push_snapshot 현재 호출:
```python
hub.push_snapshot(engine.states)   # prices, pending_codes 전달 없음 — 수정 필요
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hub.py — QueueFull 수정 + push_snapshot 시그니처 확장 (INFRA-01, INFRA-02)</name>
  <files>mutrade/admin/hub.py</files>
  <behavior>
    - push_snapshot()이 prices=None, pending_codes=None 기본값으로 하위 호환됨
    - push_snapshot()이 current_price, drop_pct, sell_pending 필드를 직렬화에 포함함
    - drop_pct = round(((current - peak) / peak) * 100, 2), peak=0 또는 current=0이면 0.0
    - sell_pending = code in pending_codes (pending_codes가 None이면 False)
    - _put_snapshot()이 큐가 full일 때 get_nowait()로 기존 항목 드롭 후 put_nowait 실행
    - _put_snapshot()이 QueueFull 예외를 catch하여 무시 (극히 드문 경합 방어)
    - call_soon_threadsafe가 put_nowait 대신 _put_snapshot을 스케줄링함
  </behavior>
  <action>
push_snapshot() 시그니처를 다음으로 변경 (INFRA-01, per D-context):
```
def push_snapshot(
    self,
    states: dict,
    prices: dict[str, float] | None = None,
    pending_codes: frozenset[str] | None = None,
) -> None:
```

직렬화 루프에서 current_price, drop_pct, sell_pending 필드 추가:
- _prices = prices or {}
- _pending = pending_codes or frozenset()
- current = _prices.get(code, 0.0)
- peak = getattr(s, 'peak_price', 0.0)
- drop = round(((current - peak) / peak) * 100, 2) if peak > 0 and current > 0 else 0.0
- sell_pending = code in _pending

call_soon_threadsafe 대상을 기존 put_nowait 람다에서 _put_snapshot 메서드로 교체 (INFRA-02):
```python
def _put_snapshot(self, data: dict) -> None:
    """asyncio 이벤트 루프 스레드에서 실행 (call_soon_threadsafe 경유)."""
    assert self._change_queue is not None
    if self._change_queue.full():
        try:
            self._change_queue.get_nowait()
        except asyncio.QueueEmpty:
            pass
    try:
        self._change_queue.put_nowait(data)
    except asyncio.QueueFull:
        pass

# push_snapshot() 내부:
self._loop.call_soon_threadsafe(self._put_snapshot, dict(serialized))
```

주의: SymbolState에 current_price 필드 직접 추가 금지 — prices dict에서 가져오는 방식 유지 (RESEARCH.md Anti-Patterns 참조).
  </action>
  <verify>
    <automated>cd /Users/sean/Study/MuTrade/MuTrade && python -c "
import asyncio, threading
from mutrade.admin.hub import BotStateHub

class FakeState:
    def __init__(self, code, peak): self.code=code; self.peak_price=peak; self.warm=True

hub = BotStateHub()
loop = asyncio.new_event_loop()
t = threading.Thread(target=loop.run_forever, daemon=True)
t.start()
hub.attach_loop(loop)

# 기본 시그니처 테스트
states = {'005930': FakeState('005930', 86200.0)}
prices = {'005930': 84500.0}
pending = frozenset({'005930'})
hub.push_snapshot(states, prices, pending)
import time; time.sleep(0.05)
snap = hub.get_snapshot()
assert 'current_price' in snap['005930'], f'current_price 없음: {snap}'
assert snap['005930']['current_price'] == 84500.0, f'wrong current_price: {snap}'
assert snap['005930']['drop_pct'] == round(((84500-86200)/86200)*100, 2)
assert snap['005930']['sell_pending'] == True

# QueueFull 테스트: 연속 2회 push — 예외 없어야 함
hub.push_snapshot(states, prices, pending)
hub.push_snapshot(states, prices, pending)
time.sleep(0.05)

# 하위 호환: prices/pending_codes 없이 호출 가능
hub.push_snapshot(states)
time.sleep(0.05)
snap2 = hub.get_snapshot()
assert snap2['005930']['current_price'] == 0.0

loop.call_soon_threadsafe(loop.stop)
print('PASS')
"
</automated>
  </verify>
  <done>
    - hub.push_snapshot(states, prices=None, pending_codes=None) 시그니처로 변경됨
    - get_snapshot() 반환값에 current_price, drop_pct, sell_pending 필드 포함
    - 연속 push 시 asyncio.QueueFull 예외 없이 최신 스냅샷으로 교체됨
    - prices/pending_codes 생략 시 current_price=0.0, sell_pending=False로 하위 호환됨
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: order_executor.py + scheduler.py — pending_codes() 노출 + 호출 갱신 (INFRA-01)</name>
  <files>
    mutrade/executor/order_executor.py
    mutrade/monitor/scheduler.py
  </files>
  <behavior>
    - OrderExecutor.pending_codes()가 frozenset[str]을 반환한다
    - _pending에 '005930'이 있을 때 pending_codes()가 frozenset({'005930'})을 반환한다
    - _pending이 비었을 때 pending_codes()가 frozenset()을 반환한다
    - scheduler.py의 hub.push_snapshot() 호출이 prices와 executor.pending_codes()를 전달한다
  </behavior>
  <action>
order_executor.py에 pending_codes() 공개 메서드 추가 (CONTEXT.md Claude's Discretion):
```python
def pending_codes(self) -> frozenset[str]:
    """현재 SELL_PENDING 중인 종목 코드 집합. 스레드 안전 복사본."""
    return frozenset(self._pending)
```

위치: execute() 메서드 바로 다음에 추가.

scheduler.py — run_session() 내 hub 연동 블록 수정:
기존:
```python
if hub is not None:
    hub.push_snapshot(engine.states)
    hub.set_running(True)
```
변경:
```python
if hub is not None:
    hub.push_snapshot(engine.states, prices, executor.pending_codes())
    hub.set_running(True)
```

주의: prices 변수는 이미 poll_prices()의 반환값으로 직상단 라인에 있음 — 변수명 그대로 사용.
  </action>
  <verify>
    <automated>cd /Users/sean/Study/MuTrade/MuTrade && python -c "
from mutrade.executor.order_executor import OrderExecutor

# pending_codes() 메서드 존재 및 반환 타입 확인
import inspect
assert hasattr(OrderExecutor, 'pending_codes'), 'pending_codes 메서드 없음'

# 실제 동작: _pending에 직접 삽입하여 반환값 확인
from unittest.mock import MagicMock
kis_mock = MagicMock()
ex = OrderExecutor(kis=kis_mock)
assert ex.pending_codes() == frozenset()
ex._pending.add('005930')
result = ex.pending_codes()
assert result == frozenset({'005930'}), f'wrong: {result}'
assert isinstance(result, frozenset), f'type: {type(result)}'

# scheduler.py 코드 텍스트 확인
import pathlib
src = pathlib.Path('mutrade/monitor/scheduler.py').read_text()
assert 'executor.pending_codes()' in src, 'scheduler.py에 pending_codes() 호출 없음'
assert 'push_snapshot(engine.states, prices, executor.pending_codes())' in src, 'push_snapshot 시그니처 갱신 안됨'
print('PASS')
"
</automated>
  </verify>
  <done>
    - OrderExecutor.pending_codes() → frozenset[str] 반환
    - scheduler.py의 push_snapshot() 호출에 prices와 executor.pending_codes() 포함됨
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APScheduler thread → asyncio Queue | 스케줄러 스레드가 loop.call_soon_threadsafe()로 asyncio 큐에 삽입 |
| hub.get_snapshot() → WebSocket → Browser | 서버 상태 데이터가 브라우저로 노출됨 (Wave 2에서 구현) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | hub._snapshot (RLock) | accept | 스냅샷은 읽기 전용 내부 상태. 외부 write 경로 없음. RLock으로 race 방어됨. |
| T-06-02 | Denial of Service | asyncio.Queue QueueFull | mitigate | 드롭-앤-리플레이스 패턴 적용 — 이번 플랜에서 수정. 무한 누적 없음. |
| T-06-03 | Information Disclosure | pending_codes() frozenset | accept | 개인용 로컬 봇. 종목 코드 노출은 허용 범위. 다중 사용자 환경 아님. |
</threat_model>

<verification>
```bash
cd /Users/sean/Study/MuTrade/MuTrade
python -m pytest tests/ -x -q --tb=short 2>/dev/null || true
python -c "from mutrade.admin.hub import BotStateHub; from mutrade.executor.order_executor import OrderExecutor; print('imports OK')"
```
</verification>

<success_criteria>
- hub.push_snapshot(states, prices, pending_codes) 시그니처 동작 확인
- get_snapshot() 반환값에 current_price, drop_pct, sell_pending 포함
- 연속 push 2회 시 QueueFull 예외 없음
- executor.pending_codes() frozenset 반환 확인
- scheduler.py에 prices + pending_codes() 전달 코드 포함
</success_criteria>

<output>
완료 후 `.planning/phases/06-모니터링-대시보드/06-01-SUMMARY.md` 생성.
</output>
