---
phase: 06-모니터링-대시보드
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - mutrade/admin/static/index.html
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "브라우저에서 각 종목의 현재가, 고점, 하락률을 행 단위로 확인할 수 있다"
    - "SELL_PENDING 종목이 번짙이는 빨간 배경 행으로 시각적으로 구분된다"
    - "페이지 새로고침 없이 WebSocket 메시지 수신 시 테이블이 자동 갱신된다"
    - "WebSocket 연결 끊김 시 자동 재연결이 시도되고 상단에 상태 표시가 갱신된다"
    - "종목 없을 때(빈 객체 수신) '봇 대기 중' 메시지가 표시된다"
  artifacts:
    - path: "mutrade/admin/static/index.html"
      provides: "단일 파일 대시보드 — HTML + 인라인 CSS + 인라인 JS"
      contains: "blink-sell"
  key_links:
    - from: "index.html WebSocket onmessage"
      to: "renderTable(data)"
      via: "JSON.parse(e.data) → renderTable()"
      pattern: "renderTable"
    - from: "index.html"
      to: "/ws"
      via: "new WebSocket(`ws://${location.host}/ws`)"
      pattern: "location.host.*ws"
---

<objective>
단일 파일 실시간 모니터링 대시보드 구현.

Purpose: 사용자가 브라우저를 열면 즉시 현재 모니터링 상태를 확인할 수 있어야 한다. npm 없이 단일 index.html로 전체 UI를 구현한다. WebSocket으로 자동 갱신되며 연결이 끊어져도 자동 복구된다.

Output: mutrade/admin/static/index.html — HTML + 인라인 CSS + 인라인 JS 단일 파일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-모니터링-대시보드/06-CONTEXT.md
@.planning/phases/06-모니터링-대시보드/06-UI-SPEC.md
@.planning/phases/06-모니터링-대시보드/06-02-SUMMARY.md

<interfaces>
<!-- WebSocket 데이터 계약 (06-UI-SPEC.md WebSocket 데이터 계약 섹션) -->

WebSocket 메시지 형식 (JSON):
```json
{
  "005930": {
    "code": "005930",
    "peak_price": 86200.0,
    "current_price": 84500.0,
    "drop_pct": -1.97,
    "warm": true,
    "sell_pending": false
  }
}
```
빈 객체 {} → 빈 상태 메시지 렌더링
sell_pending: true → tr.sell-pending 클래스 적용
warm: false → 상태 배지 "워밍업", 하락률 표시 "—"

상태 배지 텍스트 매핑:
- MONITORING → "모니터링 중"
- SELL_PENDING → "매도 대기"  (sell_pending: true 시 이 텍스트)
- WARMING_UP → "워밍업"  (warm: false 시 이 텍스트)
- 기타 → 값 그대로

색상 토큰:
- 배경: #111827 (body), #1f2937 (table/header)
- 텍스트: #f9fafb (primary), #9ca3af (secondary), #6b7280 (muted)
- 액센트: #3b82f6 (WebSocket 연결 점)
- 행 hover: #374151
- 테두리: #374151
- SELL_PENDING text: #dc2626
- blink-sell: #450a0a ↔ #7f1d1d

하락률 숫자 색상:
- 0% ~ -5%: #9ca3af
- -5% ~ -9%: #f59e0b  
- -9% 이상 하락: #ef4444
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html — 단일 파일 대시보드 구현 (DASH-01, DASH-02, DASH-03)</name>
  <files>mutrade/admin/static/index.html</files>
  <action>
`mutrade/admin/static/index.html`을 아래 명세에 따라 생성한다. 외부 CDN, 외부 폰트, 분리 파일 일체 금지 (D-01, D-02).

### HTML 구조

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MuTrade 모니터링</title>
  <style>/* 인라인 CSS */</style>
</head>
<body>
  <!-- header bar -->
  <div id="header">
    <span id="title">MuTrade 모니터링</span>
    <span id="ws-status">
      <span id="ws-dot"></span>
      <span id="ws-text">연결 중...</span>
    </span>
  </div>

  <!-- main container -->
  <div id="container">
    <table id="symbol-table">
      <thead>
        <tr>
          <th>종목코드</th>
          <th>현재가</th>
          <th>고점</th>
          <th>하락률</th>
          <th>상태</th>
        </tr>
      </thead>
      <tbody id="symbol-table-body"></tbody>
    </table>
    <div id="empty-state" style="display:none">
      <p id="empty-main">봇 대기 중</p>
      <p id="empty-sub">장 운영 시간(09:00~15:30 KST)에 자동 시작됩니다</p>
    </div>
  </div>
  <script>/* 인라인 JS */</script>
</body>
</html>
```

### CSS 명세 (UI-SPEC.md 기준)

```css
* { font-family: system-ui, -apple-system, 'Segoe UI', sans-serif; box-sizing: border-box; margin: 0; padding: 0; }

body { background: #111827; color: #f9fafb; }

#header {
  background: #1f2937;
  padding: 16px 24px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
#title { font-size: 20px; font-weight: 600; line-height: 1.2; }
#ws-status { display: flex; align-items: center; gap: 8px; font-size: 12px; color: #9ca3af; }
#ws-dot { width: 8px; height: 8px; border-radius: 50%; background: #6b7280; }
#ws-dot.connected { background: #3b82f6; }
#ws-dot.reconnecting { background: #f59e0b; }

#container { max-width: 800px; margin: 32px auto; padding: 0 24px; }

#symbol-table { width: 100%; border-collapse: collapse; background: #1f2937; table-layout: fixed; }
#symbol-table th, #symbol-table td {
  padding: 8px 12px;
  font-size: 14px;
  border-bottom: 1px solid #374151;
  text-align: right;
}
#symbol-table th { font-weight: 600; color: #9ca3af; line-height: 1.4; }
#symbol-table td { line-height: 1.5; }
#symbol-table td:first-child, #symbol-table th:first-child { text-align: left; width: 100px; }
#symbol-table th:nth-child(2), #symbol-table td:nth-child(2) { width: 120px; }
#symbol-table th:nth-child(3), #symbol-table td:nth-child(3) { width: 120px; }
#symbol-table th:nth-child(4), #symbol-table td:nth-child(4) { width: 100px; }
#symbol-table th:nth-child(5), #symbol-table td:nth-child(5) { width: 120px; text-align: center; }
#symbol-table tbody tr:hover { background: #374151; }

/* SELL_PENDING 번짙임 (D-05) */
@keyframes blink-sell {
  0%, 100% { background-color: #450a0a; }
  50%       { background-color: #7f1d1d; }
}
tr.sell-pending { animation: blink-sell 1s ease-in-out infinite; }
tr.sell-pending td { color: #fca5a5; }
tr.sell-pending .status-badge { color: #ef4444; font-weight: 600; }

/* 빈 상태 (D-06) */
#empty-state { text-align: center; padding: 48px 0; color: #9ca3af; }
#empty-main { font-size: 20px; font-weight: 600; margin-bottom: 8px; }
#empty-sub { font-size: 12px; color: #6b7280; }
```

### JS 명세

```javascript
let ws;

function getDropColor(dropPct) {
  // UI-SPEC.md 하락률 숫자 색상
  if (dropPct <= -9) return '#ef4444';
  if (dropPct <= -5) return '#f59e0b';
  return '#9ca3af';
}

function fmtPrice(v) {
  // UI-SPEC.md: 84,500원 포맷
  if (!v || v === 0) return '—';
  return v.toLocaleString('ko-KR') + '원';
}

function fmtDrop(v, warm) {
  // UI-SPEC.md: warm=false 시 '—', 그 외 -3.25% 포맷
  if (!warm) return '—';
  if (v === null || v === undefined) return '—';
  return v.toFixed(2) + '%';
}

function getStatusText(sym) {
  // UI-SPEC.md 상태 배지 텍스트 매핑
  if (sym.sell_pending) return '매도 대기';
  if (!sym.warm) return '워밍업';
  return '모니터링 중';
}

function setStatus(state) {
  // state: 'connected' | 'reconnecting' | 'disconnected'
  const dot = document.getElementById('ws-dot');
  const text = document.getElementById('ws-text');
  dot.className = state === 'connected' ? 'connected' : (state === 'reconnecting' ? 'reconnecting' : '');
  const labels = { connected: '실시간 연결', reconnecting: '재연결 중...', disconnected: '연결 끊김' };
  text.textContent = labels[state] || '연결 중...';
}

function renderTable(data) {
  const tbody = document.getElementById('symbol-table-body');
  const emptyState = document.getElementById('empty-state');
  const table = document.getElementById('symbol-table');
  const keys = Object.keys(data);
  if (keys.length === 0) {
    tbody.innerHTML = '';
    table.style.display = 'none';
    emptyState.style.display = 'block';
    return;
  }
  table.style.display = '';
  emptyState.style.display = 'none';
  tbody.innerHTML = keys.map(code => {
    const sym = data[code];
    const isPending = sym.sell_pending;
    const dropColor = getDropColor(sym.drop_pct);
    return `<tr class="${isPending ? 'sell-pending' : ''}">
      <td>${sym.code}</td>
      <td>${fmtPrice(sym.current_price)}</td>
      <td>${fmtPrice(sym.peak_price)}</td>
      <td style="color:${dropColor}">${fmtDrop(sym.drop_pct, sym.warm)}</td>
      <td><span class="status-badge">${getStatusText(sym)}</span></td>
    </tr>`;
  }).join('');
}

function connect() {
  // D-03: setTimeout 자동 재연결
  ws = new WebSocket(`ws://${location.host}/ws`);
  ws.onopen = () => setStatus('connected');
  ws.onmessage = (e) => { try { renderTable(JSON.parse(e.data)); } catch(err) { console.error('parse error', err); } };
  ws.onclose = () => { setStatus('reconnecting'); setTimeout(connect, 3000); };
  ws.onerror = () => ws.close();
}

connect();
```

주의:
- 외부 CDN 스크립트 태그 일체 금지 (UI-SPEC.md Registry Safety)
- 분리 CSS/JS 파일 금지 — 모든 스타일·스크립트 인라인 (D-02)
- `drop_pct`는 서버에서 이미 % 단위 float으로 전달 (예: -1.97). toFixed(2)+'%' 적용.
- `fmtDrop`에서 warm=false이면 하락률 '—' 표시 (UI-SPEC.md)
  </action>
  <verify>
    <automated>cd /Users/sean/Study/MuTrade/MuTrade && python -c "
import pathlib, re

html = pathlib.Path('mutrade/admin/static/index.html').read_text()

# 핵심 요소 존재 확인
assert 'MuTrade 모니터링' in html, '제목 없음'
assert 'symbol-table-body' in html, 'tbody id 없음'
assert 'blink-sell' in html, 'SELL_PENDING 애니메이션 없음'
assert '봇 대기 중' in html, '빈 상태 메시지 없음'
assert '장 운영 시간(09:00~15:30 KST)' in html, '빈 상태 부제 없음'
assert 'renderTable' in html, 'renderTable 함수 없음'
assert 'location.host' in html, 'WebSocket 연결 코드 없음'
assert 'setTimeout(connect' in html, '자동 재연결 없음'
assert 'sell-pending' in html, 'sell-pending 클래스 없음'
assert '실시간 연결' in html, 'WS 연결 상태 텍스트 없음'
assert '재연결 중' in html, 'WS 재연결 텍스트 없음'
assert '#450a0a' in html, 'blink-sell 다크 레드 색상 없음'
assert '매도 대기' in html, '상태 배지 텍스트 없음'
assert '모니터링 중' in html, '상태 배지 텍스트 없음'
assert '워밍업' in html, '워밍업 상태 없음'

# 외부 CDN 없음 확인
cdn_patterns = ['cdn.jsdelivr', 'unpkg.com', 'cdnjs', 'googleapis.com/css', 'bootstrapcdn']
for p in cdn_patterns:
    assert p not in html, f'외부 CDN 감지됨: {p}'

print('PASS: index.html 검증 완료')
"
</automated>
  </verify>
  <done>
    - mutrade/admin/static/index.html 생성됨
    - HTML + 인라인 CSS + 인라인 JS 단일 파일 (외부 의존성 없음)
    - 테이블 5열 구성: 종목코드, 현재가, 고점, 하락률, 상태
    - SELL_PENDING 행에 blink-sell 애니메이션 적용 (tr.sell-pending)
    - 빈 상태 "봇 대기 중" 메시지 + 부제 표시
    - WebSocket 자동 재연결 (setTimeout 3초)
    - 연결 상태 dot + 텍스트 표시
    - 하락률 색상 3단계 (정상/경고/임계)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 1~3 전체 완료: 인프라 버그 수정 + WebSocket 엔드포인트 + 대시보드 UI
  </what-built>
  <how-to-verify>
    1. 봇 실행: `python -m mutrade` 또는 `uvicorn mutrade.admin.app:create_app --factory --host 0.0.0.0 --port 8000`
    2. 브라우저에서 http://localhost:8000 접속
    3. 확인 항목:
       - 헤더: "MuTrade 모니터링" 제목 + 파란 점 "실시간 연결" 표시 (WebSocket 연결됨)
       - 봇 미실행 상태: "봇 대기 중" 메시지 표시, 테이블 숨김
       - 봇 실행 중 상태: 종목 행이 현재가/고점/하락률 열로 표시됨
       - SELL_PENDING 종목(있는 경우): 행이 어두운 빨간 배경으로 번짙임
       - 하락률 색상: -5% 미만 gray, -5~-9% amber, -9% 이상 red
    4. 개발자도구 Console에 오류 없음 확인
    5. Network 탭 → WS → ws://localhost:8000/ws 연결 확인 및 메시지 수신 확인
    6. 페이지 열어두고 새 폴링 주기 대기 → 테이블 자동 갱신 확인 (새로고침 불필요)
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 이슈 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser WebSocket → index.html DOM | WebSocket 메시지가 DOM에 삽입됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering / XSS | renderTable innerHTML 삽입 | mitigate | 서버 스냅샷의 code 필드는 KIS 종목코드(6자리 숫자) — 서버 직렬화에서 getattr(s, 'code', code) 패턴 사용. 사용자 입력 경유 없음. 개인용 로컬 봇 — XSS 공격 벡터 없음. 단, innerHTML 대신 textContent 사용을 권장하나 템플릿 행 구성상 innerHTML 사용이 불가피한 경우 코드 필드를 `sym.code.replace(/[^0-9A-Za-z]/g, '')` 로 sanitize. |
| T-06-09 | Denial of Service | 브라우저 탭 WebSocket 무한 루프 | accept | setTimeout(connect, 3000) 패턴은 서버 다운 시 3초 간격 재연결. 개인용 단일 클라이언트 — DOS 위험 없음. |
| T-06-10 | Information Disclosure | 외부 CDN 로드 | mitigate | UI-SPEC.md Registry Safety 준수 — 외부 CDN 완전 차단. 단일 파일 자급자족 원칙. verify 스크립트로 CDN 패턴 검출. |
</threat_model>

<verification>
```bash
cd /Users/sean/Study/MuTrade/MuTrade
# index.html 정적 검증
python -c "
import pathlib
html = pathlib.Path('mutrade/admin/static/index.html').read_text()
checks = ['blink-sell', 'renderTable', 'sell-pending', '봇 대기 중', 'setTimeout(connect']
for c in checks:
    assert c in html, f'FAIL: {c} not found'
print('index.html 정적 검증 PASS')
"

# FastAPI 앱 전체 라우트 확인
python -c "
from mutrade.admin.app import create_app
from mutrade.admin.hub import BotStateHub
app = create_app(BotStateHub())
routes = {getattr(r,'path','?') for r in app.routes}
assert '/' in routes and '/ws' in routes
print('FastAPI 라우트 PASS')
"
```
</verification>

<success_criteria>
- index.html 존재 및 핵심 요소 포함 (blink-sell, renderTable, 봇 대기 중, setTimeout 재연결)
- 외부 CDN 로드 코드 없음
- 브라우저에서 http://localhost:8000 접속 시 대시보드 렌더링 확인 (human-verify)
- WebSocket 자동 갱신 동작 확인 (human-verify)
</success_criteria>

<output>
완료 후 `.planning/phases/06-모니터링-대시보드/06-03-SUMMARY.md` 생성.
</output>
