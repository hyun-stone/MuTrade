---
phase: 07-봇-제어
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mutrade/admin/hub.py
  - mutrade/admin/app.py
  - mutrade/monitor/scheduler.py
  - tests/test_app_routes.py
  - tests/test_hub.py
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
must_haves:
  truths:
    - "POST /api/start 호출 시 시장 시간이면 200 OK, 아니면 400, 이미 실행 중이면 409"
    - "POST /api/stop 호출 시 hub.request_stop()이 실행된다"
    - "POST /api/toggle-dry-run 호출 시 engine._dry_run과 executor._dry_run이 반전된다"
    - "POST /api/sell/{code} 호출 시 executor.execute()가 dry_run=False SellSignal로 실행된다"
    - "WebSocket 스냅샷에 _meta.is_running과 _meta.dry_run 필드가 포함된다"
  artifacts:
    - path: "mutrade/admin/app.py"
      provides: "4개 POST 엔드포인트 (/api/start, /api/stop, /api/toggle-dry-run, /api/sell/{code})"
      contains: "async def api_start"
    - path: "mutrade/admin/hub.py"
      provides: "push_snapshot에 dry_run 인자 추가, _meta 최상위 필드"
      contains: "dry_run: bool = False"
    - path: "mutrade/monitor/scheduler.py"
      provides: "push_snapshot 호출에 dry_run=engine._dry_run 인자 전달"
      contains: "dry_run=engine._dry_run"
    - path: "tests/test_app_routes.py"
      provides: "TestControlRoutes 클래스 — 7개 테스트"
      contains: "class TestControlRoutes"
    - path: "tests/test_hub.py"
      provides: "TestBotStateHubPhase7 클래스 — 4개 테스트"
      contains: "class TestBotStateHubPhase7"
  key_links:
    - from: "mutrade/admin/app.py"
      to: "mutrade/admin/hub.py"
      via: "request.app.state.hub"
      pattern: "request\\.app\\.state\\.hub"
    - from: "mutrade/admin/app.py"
      to: "mutrade/engine/models.py"
      via: "SellSignal import"
      pattern: "from mutrade\\.engine\\.models import SellSignal"
    - from: "mutrade/monitor/scheduler.py"
      to: "mutrade/admin/hub.py"
      via: "hub.push_snapshot(dry_run=engine._dry_run)"
      pattern: "dry_run=engine\\._dry_run"
---

<objective>
Phase 7 백엔드: 봇 제어 API 엔드포인트 4개를 FastAPI 앱에 추가하고, hub.push_snapshot()에 dry_run 메타 필드를 포함시킨다.

Purpose: 프론트엔드(Plan 02)가 호출할 REST API와 WebSocket 스냅샷 계약을 완성한다. 시작/중지/드라이런 토글/수동 매도의 서버 측 로직이 모두 동작하고 테스트로 검증된 상태를 만든다.
Output: app.py에 4개 POST 라우트, hub.py에 _meta 필드 포함 스냅샷, scheduler.py에 dry_run 인자 전달, 테스트 11개 GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-봇-제어/07-CONTEXT.md
@.planning/phases/07-봇-제어/07-RESEARCH.md
@.planning/phases/07-봇-제어/07-PATTERNS.md

<interfaces>
<!-- 실행자가 참조해야 할 기존 코드 계약 -->

From mutrade/engine/models.py:
```python
@dataclass(frozen=True)
class SellSignal:
    code: str
    name: str
    current_price: float
    peak_price: float
    drop_pct: float
    threshold: float
    dry_run: bool
```

From mutrade/admin/hub.py:
```python
class BotStateHub:
    def __init__(self) -> None:
        self._lock = threading.RLock()
        self._snapshot: dict = {}
        self._is_running: bool = False

    def push_snapshot(self, states, prices=None, pending_codes=None) -> None: ...
    def get_snapshot(self) -> dict: ...
    def request_stop(self) -> None: ...
    def clear_stop(self) -> None: ...
    def is_stop_requested(self) -> bool: ...
    def set_running(self, running: bool) -> None: ...
    def is_running(self) -> bool: ...
```

From mutrade/admin/app.py:
```python
def create_app(hub: BotStateHub, **kwargs: Any) -> FastAPI:
    # lifespan 내에서 app.state.hub = hub
    # kwargs에 scheduler, engine, executor, config 주입 가능
```

From mutrade/executor/order_executor.py:
```python
class OrderExecutor:
    def __init__(self, ..., dry_run: bool = False):
        self._dry_run = dry_run
        self._pending: set[str] = set()
    def execute(self, signal: SellSignal) -> None: ...
    def pending_codes(self) -> frozenset[str]: ...
```

From mutrade/engine/trailing_stop.py:
```python
class TrailingStopEngine:
    def __init__(self, ..., dry_run: bool = False):
        self._dry_run = dry_run
```

From mutrade/config/loader.py (AppConfig.symbols):
```python
# config.symbols = list of SymbolConfig
# SymbolConfig has: code, name, threshold
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: hub.py에 _meta 필드 추가 + scheduler.py dry_run 인자 전달 + 테스트</name>
  <files>mutrade/admin/hub.py, mutrade/monitor/scheduler.py, tests/test_hub.py</files>

  <read_first>
    - mutrade/admin/hub.py (현재 push_snapshot 시그니처와 _put_snapshot 로직)
    - mutrade/monitor/scheduler.py (hub.push_snapshot 호출 위치 — line 116)
    - tests/test_hub.py (기존 TestBotStateHubPhase6 클래스 구조)
  </read_first>

  <action>
**hub.py 수정:**

1. `push_snapshot()` 시그니처에 `dry_run: bool = False` 인자 추가:
```python
def push_snapshot(
    self,
    states: dict,
    prices: "dict[str, float] | None" = None,
    pending_codes: "frozenset[str] | None" = None,
    dry_run: bool = False,
) -> None:
```

2. `with self._lock:` 블록 내에서 `self._snapshot`에 `_meta` 최상위 키 추가. 기존 종목별 직렬화 dict(`serialized`)와 병합:
```python
with self._lock:
    self._snapshot = {
        "_meta": {
            "is_running": self._is_running,
            "dry_run": dry_run,
        },
        **serialized,
    }
```

3. `_put_snapshot()` 호출에도 동일하게 `_meta` 포함된 dict 전달 (line 79 부근):
```python
self._loop.call_soon_threadsafe(
    self._put_snapshot, {"_meta": {"is_running": self._is_running, "dry_run": dry_run}, **serialized}
)
```

**scheduler.py 수정:**

1. `run_session()` 내 `hub.push_snapshot()` 호출(line 116)에 `dry_run=engine._dry_run` 인자 추가:
```python
hub.push_snapshot(engine.states, prices, executor.pending_codes(), dry_run=engine._dry_run)
```

**tests/test_hub.py 수정:**

1. 파일 끝에 `TestBotStateHubPhase7` 클래스 추가. 4개 테스트:
   - `test_push_snapshot_with_dry_run_true`: `push_snapshot(states, dry_run=True)` 후 `get_snapshot()["_meta"]["dry_run"]`이 `True`
   - `test_push_snapshot_with_dry_run_false`: `push_snapshot(states, dry_run=False)` 후 `get_snapshot()["_meta"]["dry_run"]`이 `False`
   - `test_push_snapshot_dry_run_default_false`: `push_snapshot(states)` (인자 없이) 후 `get_snapshot()["_meta"]["dry_run"]`이 `False` — 하위 호환성
   - `test_meta_contains_is_running`: `hub.set_running(True)` 후 `push_snapshot(states)` → `get_snapshot()["_meta"]["is_running"]`이 `True`

2. 테스트 내 `states` 구성: `{"005930": SymbolState(code="005930", peak_price=86200.0, warm=True)}` — 기존 TestBotStateHubPhase6의 `_make_fake_state` 패턴 참조하되 실제 `SymbolState` dataclass import:
```python
from mutrade.engine.models import SymbolState
```
  </action>

  <verify>
    <automated>python3.11 -m pytest tests/test_hub.py::TestBotStateHubPhase7 -x -q</automated>
  </verify>

  <acceptance_criteria>
    - hub.py의 `push_snapshot` 시그니처에 `dry_run: bool = False` 인자가 존재한다
    - hub.py에 `"_meta"` 문자열이 포함된다 (스냅샷 최상위 키)
    - hub.py에 `"is_running"` 과 `"dry_run"` 문자열이 `_meta` dict 내에 포함된다
    - scheduler.py에 `dry_run=engine._dry_run` 문자열이 포함된다
    - tests/test_hub.py에 `class TestBotStateHubPhase7` 가 존재한다
    - `python3.11 -m pytest tests/test_hub.py::TestBotStateHubPhase7 -x -q` 가 4 passed로 종료한다
    - `python3.11 -m pytest tests/test_hub.py -x -q` 가 기존 테스트 포함 전체 통과한다 (하위 호환 확인)
  </acceptance_criteria>

  <done>push_snapshot()이 _meta.is_running과 _meta.dry_run을 포함하는 스냅샷을 생성하고, scheduler.py가 dry_run 인자를 전달하며, 4개 테스트가 모두 GREEN</done>
</task>

<task type="auto">
  <name>Task 2: app.py에 4개 POST 엔드포인트 추가 + app.state 주입 + 테스트</name>
  <files>mutrade/admin/app.py, tests/test_app_routes.py</files>

  <read_first>
    - mutrade/admin/app.py (현재 create_app 구조, lifespan, 기존 라우트 패턴)
    - mutrade/executor/order_executor.py (execute 메서드 시그니처, _dry_run 속성, pending_codes)
    - mutrade/engine/trailing_stop.py (TrailingStopEngine._dry_run 속성)
    - mutrade/engine/models.py (SellSignal dataclass 필드)
    - mutrade/config/loader.py (AppConfig.symbols 구조, SymbolConfig.code/name)
    - tests/test_app_routes.py (기존 fixture 패턴, TestClient 사용법)
    - mutrade/admin/hub.py (Task 1에서 수정된 get_snapshot 반환값 — _meta 포함)
  </read_first>

  <action>
**app.py 수정:**

1. **import 추가** (기존 import 블록에 병합):
```python
import re
from datetime import datetime, timezone
from zoneinfo import ZoneInfo
from fastapi import FastAPI, WebSocket, WebSocketDisconnect, Request, HTTPException
from fastapi.concurrency import run_in_threadpool
from mutrade.engine.models import SellSignal
```
기존 `from fastapi import FastAPI, WebSocket, WebSocketDisconnect` 라인을 위의 확장 버전으로 교체.

2. **KST 상수** (모듈 레벨, STATIC_DIR 아래):
```python
KST = ZoneInfo("Asia/Seoul")
```

3. **lifespan 내 app.state 주입 확장** (기존 `app.state.hub = hub` 아래에 추가):
```python
app.state.scheduler = kwargs.get("scheduler")
app.state.engine = kwargs.get("engine")
app.state.executor = kwargs.get("executor")
app.state.config = kwargs.get("config")
```

4. **POST /api/start** (per D-01, D-02, D-03):
```python
@app.post("/api/start")
async def api_start(request: Request):
    _hub: BotStateHub = request.app.state.hub
    scheduler = request.app.state.scheduler

    if _hub.is_running():
        raise HTTPException(status_code=409, detail="이미 실행 중입니다")

    now_kst = datetime.now(KST)
    current_min = now_kst.hour * 60 + now_kst.minute
    if not (9 * 60 <= current_min < 15 * 60 + 20):
        raise HTTPException(
            status_code=400,
            detail="시장 시간이 아닙니다 (09:00~15:20 KST)",
        )

    _hub.clear_stop()
    scheduler.modify_job("market_poll", next_run_time=datetime.now(timezone.utc))
    logger.info("Admin UI: 봇 시작 요청 — modify_job 발화")
    return {"ok": True, "message": "모니터링 세션 시작됨"}
```

5. **POST /api/stop** (per D-04):
```python
@app.post("/api/stop")
async def api_stop(request: Request):
    _hub: BotStateHub = request.app.state.hub
    _hub.request_stop()
    logger.info("Admin UI: 봇 중지 요청")
    return {"ok": True, "message": "중지 요청됨"}
```

6. **POST /api/toggle-dry-run** (per D-07, D-08, D-09):
```python
@app.post("/api/toggle-dry-run")
async def api_toggle_dry_run(request: Request):
    _hub: BotStateHub = request.app.state.hub
    engine = request.app.state.engine
    executor = request.app.state.executor

    with _hub._lock:
        new_val = not engine._dry_run
        engine._dry_run = new_val
        executor._dry_run = new_val

    direction = "드라이런" if new_val else "실매도"
    logger.info("Admin UI: dry_run={}", new_val)
    return {
        "ok": True,
        "dry_run": new_val,
        "message": f"{direction} 모드로 전환됨. 재시작 시 .env 설정으로 초기화됩니다",
    }
```

7. **POST /api/sell/{code}** (per D-10, D-11, D-12):
```python
@app.post("/api/sell/{code}")
async def api_sell(code: str, request: Request):
    if not re.match(r"^[0-9A-Za-z]{1,12}$", code):
        raise HTTPException(status_code=400, detail="유효하지 않은 종목 코드")

    _hub: BotStateHub = request.app.state.hub
    executor = request.app.state.executor
    _config = request.app.state.config

    snapshot = _hub.get_snapshot()
    sym = snapshot.get(code)
    if sym is None:
        raise HTTPException(status_code=404, detail=f"종목 {code}를 찾을 수 없습니다")

    # config에서 종목명 조회, 없으면 code 사용
    name = code
    if _config is not None:
        sym_cfg = next((s for s in _config.symbols if s.code == code), None)
        if sym_cfg is not None:
            name = sym_cfg.name

    sig = SellSignal(
        code=code,
        name=name,
        current_price=sym.get("current_price", 0.0),
        peak_price=sym.get("peak_price", 0.0),
        drop_pct=sym.get("drop_pct", 0.0),
        threshold=0.0,
        dry_run=False,  # D-11: 수동 매도는 항상 실거래
    )
    await run_in_threadpool(executor.execute, sig)
    logger.warning("Admin UI: 수동 매도 주문 제출 — {}", code)
    return {"ok": True, "message": "매도 주문 제출됨 — 체결 확인 필요"}
```

위 4개 라우트는 기존 `@app.get("/health")` 라우트와 `app.mount("/static", ...)` 사이에 배치한다.

**tests/test_app_routes.py 수정:**

1. 파일 끝에 `TestControlRoutes` 클래스 추가. 테스트 픽스처:
```python
@pytest.fixture
def mock_scheduler():
    return MagicMock()

@pytest.fixture
def mock_engine():
    engine = MagicMock()
    engine._dry_run = True
    return engine

@pytest.fixture
def mock_executor():
    executor = MagicMock()
    executor._dry_run = True
    executor.execute = MagicMock()
    return executor

@pytest.fixture
def app_with_ctrl(hub, tmp_path, mock_scheduler, mock_engine, mock_executor):
    static_dir = tmp_path / "static"
    static_dir.mkdir()
    (static_dir / "index.html").write_text("<html/>")

    import mutrade.admin.app as app_module
    with patch.object(app_module, "STATIC_DIR", static_dir, create=True):
        _app = create_app(
            hub,
            scheduler=mock_scheduler,
            engine=mock_engine,
            executor=mock_executor,
        )
    return _app
```

2. `TestControlRoutes` 클래스 — 7개 테스트:

   - `test_start_ok`: `hub.set_running(False)` → `datetime.now(KST)` 를 시장 시간(10:00)으로 patch → `POST /api/start` → 200, `mock_scheduler.modify_job.assert_called_once()`
   - `test_start_409`: `hub.set_running(True)` → `POST /api/start` → 409
   - `test_start_market_closed`: `hub.set_running(False)` → `datetime.now(KST)` 를 시장 외(20:00)으로 patch → `POST /api/start` → 400, detail에 "시장 시간이 아닙니다" 포함
   - `test_stop_ok`: `POST /api/stop` → 200, `hub.is_stop_requested()` is True
   - `test_toggle_dry_run`: `mock_engine._dry_run = True` → `POST /api/toggle-dry-run` → 200, body `dry_run` is False, `mock_engine._dry_run` is False, `mock_executor._dry_run` is False
   - `test_sell_ok`: hub에 종목 스냅샷 push 후 → `POST /api/sell/005930` → 200, `mock_executor.execute.assert_called_once()`, 호출된 SellSignal의 `dry_run`이 `False`
   - `test_sell_404`: 빈 hub → `POST /api/sell/999999` → 404

3. 시장 시간 patch 방법:
```python
from unittest.mock import patch
from datetime import datetime
from zoneinfo import ZoneInfo

# 시장 시간 내(10:00 KST)로 고정
market_open_dt = datetime(2026, 4, 18, 10, 0, tzinfo=ZoneInfo("Asia/Seoul"))
with patch("mutrade.admin.app.datetime") as mock_dt:
    mock_dt.now.return_value = market_open_dt
    mock_dt.side_effect = lambda *a, **kw: datetime(*a, **kw)
    # ... test
```
주의: `datetime.now(timezone.utc)` 도 app.py에서 사용되므로 `mock_dt.now`의 side_effect를 인자 기반으로 분기하거나, `modify_job` 호출만 assert하고 `next_run_time` 인자의 정확한 값은 `ANY`로 검증.
  </action>

  <verify>
    <automated>python3.11 -m pytest tests/test_app_routes.py::TestControlRoutes -x -q</automated>
  </verify>

  <acceptance_criteria>
    - app.py에 `async def api_start` 함수가 존재한다
    - app.py에 `async def api_stop` 함수가 존재한다
    - app.py에 `async def api_toggle_dry_run` 함수가 존재한다
    - app.py에 `async def api_sell` 함수가 존재한다
    - app.py에 `from mutrade.engine.models import SellSignal` import가 존재한다
    - app.py에 `run_in_threadpool` import가 존재한다
    - app.py lifespan에 `app.state.scheduler = kwargs.get("scheduler")` 가 존재한다
    - app.py에 `scheduler.modify_job("market_poll"` 문자열이 존재한다 (trigger_job 아님)
    - app.py에 `re.match(r"^[0-9A-Za-z]{1,12}$", code)` 종목 코드 검증이 존재한다
    - tests/test_app_routes.py에 `class TestControlRoutes` 가 존재한다
    - `python3.11 -m pytest tests/test_app_routes.py::TestControlRoutes -x -q` 가 7 passed로 종료한다
    - `python3.11 -m pytest tests/ -x -q` 전체 테스트 스위트가 통과한다
  </acceptance_criteria>

  <done>FastAPI 앱에 POST /api/start, /api/stop, /api/toggle-dry-run, /api/sell/{code} 4개 엔드포인트가 동작하고, 7개 테스트가 모두 GREEN이며, 기존 테스트 스위트도 전체 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> FastAPI POST endpoints | 사용자 입력(종목코드 path param)이 서버로 전달됨 |
| FastAPI -> OrderExecutor | 수동 매도 명령이 실제 KIS API 주문으로 이어짐 |
| FastAPI -> APScheduler | 시작 명령이 스케줄러 job 즉시 발화를 트리거 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | POST /api/sell/{code} path param | mitigate | `re.match(r"^[0-9A-Za-z]{1,12}$", code)` 정규식으로 종목코드 형식 검증. 불일치 시 400 반환. |
| T-7-02 | Elevation of Privilege | POST /api/start 시장 시간 우회 | mitigate | 서버 핸들러에서 KST 시간 검증 (D-01/D-03). 클라이언트 검증만으로는 불충분. |
| T-7-03 | Tampering | POST /api/sell/{code} 중복 주문 | accept | executor._pending set이 서버 측에서 중복 방지. 프론트엔드 버튼 disabled는 UX 보완. |
| T-7-04 | Denial of Service | POST endpoints 반복 호출 | accept | 개인용 봇, 로컬 네트워크 전제. rate limiting 불필요. |
</threat_model>

<verification>
1. `python3.11 -m pytest tests/test_hub.py::TestBotStateHubPhase7 -x -q` — 4 passed
2. `python3.11 -m pytest tests/test_app_routes.py::TestControlRoutes -x -q` — 7 passed
3. `python3.11 -m pytest tests/ -x -q` — 전체 스위트 GREEN
4. grep 검증: `app.py`에 `api_start`, `api_stop`, `api_toggle_dry_run`, `api_sell` 4개 함수 존재
5. grep 검증: `hub.py`에 `"_meta"` 키 포함
</verification>

<success_criteria>
- 4개 POST API가 등록되어 TestClient로 호출 가능
- hub.push_snapshot()이 _meta 필드를 포함하는 스냅샷 생성
- scheduler.py가 dry_run 인자를 push_snapshot에 전달
- 전체 테스트 스위트 GREEN (기존 + 신규 11개)
- trigger_job() 사용하지 않음 (modify_job 사용)
- executor.execute()가 run_in_threadpool로 오프로드됨
</success_criteria>

<output>
After completion, create `.planning/phases/07-봇-제어/07-01-SUMMARY.md`
</output>
