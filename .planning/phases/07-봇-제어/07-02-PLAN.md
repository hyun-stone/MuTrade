---
phase: 07-봇-제어
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - mutrade/admin/static/index.html
autonomous: false
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
must_haves:
  truths:
    - "시작 버튼 클릭 시 POST /api/start가 호출되고 오류 시 배너 표시"
    - "중지 버튼 클릭 시 SELL_PENDING 종목이 있으면 confirm 다이얼로그 표시 후 POST /api/stop 호출"
    - "드라이런 배지 클릭 시 POST /api/toggle-dry-run 호출, 응답 후 배너 표시"
    - "각 종목 행에 즉시 매도 버튼이 표시되고 클릭 시 confirm 후 POST /api/sell/{code} 호출"
    - "WebSocket 스냅샷의 is_running에 따라 시작/중지 버튼 disabled 상태가 토글됨"
    - "WebSocket 스냅샷의 dry_run에 따라 헤더 배지가 드라이런/실매도로 갱신됨"
    - "SELL_PENDING 행의 즉시 매도 버튼이 비활성화됨"
  artifacts:
    - path: "mutrade/admin/static/index.html"
      provides: "시작/중지 버튼, 드라이런 배지, 배너, 즉시 매도 버튼, JS 핸들러"
      contains: "btn-start"
  key_links:
    - from: "index.html JS fetch('/api/start')"
      to: "app.py POST /api/start"
      via: "fetch POST"
      pattern: "fetch.*api/start"
    - from: "index.html JS fetch('/api/sell/')"
      to: "app.py POST /api/sell/{code}"
      via: "fetch POST"
      pattern: "fetch.*api/sell"
    - from: "index.html ws.onmessage"
      to: "hub.py push_snapshot _meta"
      via: "WebSocket JSON"
      pattern: "data\\._meta"
---

<objective>
Phase 7 프론트엔드: 기존 대시보드 index.html에 봇 제어 UI(시작/중지 버튼, 드라이런 배지, 배너, 즉시 매도 버튼)를 추가한다.

Purpose: Plan 01에서 완성된 4개 REST API를 브라우저에서 호출할 수 있는 UI를 제공하여, 사용자가 웹 대시보드에서 봇을 완전히 제어할 수 있게 한다.
Output: index.html에 헤더 제어 영역, 배너, 테이블 6열, JS 핸들러 추가 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-봇-제어/07-CONTEXT.md
@.planning/phases/07-봇-제어/07-UI-SPEC.md
@.planning/phases/07-봇-제어/07-PATTERNS.md
@.planning/phases/07-봇-제어/07-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 API 계약 -->

POST /api/start
  - 200: {"ok": true, "message": "모니터링 세션 시작됨"}
  - 400: {"detail": "시장 시간이 아닙니다 (09:00~15:20 KST)"}
  - 409: {"detail": "이미 실행 중입니다"}

POST /api/stop
  - 200: {"ok": true, "message": "중지 요청됨"}

POST /api/toggle-dry-run
  - 200: {"ok": true, "dry_run": bool, "message": "...모드로 전환됨. 재시작 시 .env 설정으로 초기화됩니다"}

POST /api/sell/{code}
  - 200: {"ok": true, "message": "매도 주문 제출됨 — 체결 확인 필요"}
  - 400: {"detail": "유효하지 않은 종목 코드"}
  - 404: {"detail": "종목 {code}를 찾을 수 없습니다"}

WebSocket /ws 스냅샷 형식 (Plan 01 이후):
{
  "_meta": {"is_running": bool, "dry_run": bool},
  "005930": {"code": "005930", "peak_price": 86200.0, "warm": true, "current_price": 84500.0, "drop_pct": -1.97, "sell_pending": false},
  ...
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html 헤더 제어 영역 + 배너 + 테이블 6열 + JS 핸들러 전체 구현</name>
  <files>mutrade/admin/static/index.html</files>

  <read_first>
    - mutrade/admin/static/index.html (현재 전체 파일 — HTML 구조, CSS, JS 모두 확인)
    - .planning/phases/07-봇-제어/07-UI-SPEC.md (디자인 계약 — 색상, 크기, 문구 정확 참조)
    - .planning/phases/07-봇-제어/07-PATTERNS.md (index.html 섹션 — 코드 패턴 참조)
    - .planning/phases/07-봇-제어/07-CONTEXT.md (D-01~D-13 결정 사항)
  </read_first>

  <action>
기존 `index.html`을 수정하여 Phase 7 제어 UI를 추가한다. 모든 변경은 기존 파일 내 인라인 수정이다.

**CSS 추가** (`</style>` 직전에 삽입):

```css
/* Phase 7 — 제어 UI */
#header-controls { display: flex; align-items: center; gap: 8px; }

#dry-run-badge {
  font-size: 12px; font-weight: 600; padding: 4px 8px;
  border-radius: 4px; cursor: pointer; user-select: none;
}

#btn-start, #btn-stop {
  font-size: 14px; font-weight: 600; padding: 8px 16px;
  border-radius: 6px; border: none; cursor: pointer; color: #fff;
}
#btn-start { background: #3b82f6; }
#btn-start:hover:not(:disabled) { background: #2563eb; }
#btn-stop { background: #dc2626; }
#btn-stop:hover:not(:disabled) { background: #b91c1c; }
#btn-start:disabled, #btn-stop:disabled {
  background: #374151; color: #6b7280; cursor: not-allowed;
}

#banner {
  display: none; padding: 12px 24px; font-size: 14px; font-weight: 400;
}

/* 즉시 매도 버튼 — 테이블 셀 내 */
.sell-btn {
  font-size: 12px; font-weight: 600; padding: 4px 8px;
  border-radius: 4px; border: none; cursor: pointer;
  background: #dc2626; color: #fff;
}
.sell-btn:hover:not(:disabled) { background: #b91c1c; }
.sell-btn:disabled { background: #374151; color: #6b7280; cursor: not-allowed; }

/* 테이블 6번째 열 */
#symbol-table th:nth-child(6), #symbol-table td:nth-child(6) { width: 80px; text-align: center; }
```

**HTML 헤더 수정** — 기존 `<div id="header">` 전체를 아래로 교체:

```html
<div id="header">
  <span id="title">MuTrade 모니터링</span>
  <div id="header-controls">
    <span id="dry-run-badge" onclick="toggleDryRun()"
      style="background:#78350f;color:#fbbf24;">드라이런</span>
    <button id="btn-start" onclick="startBot()">시작</button>
    <button id="btn-stop" onclick="stopBot()" disabled>중지</button>
    <span id="ws-status">
      <span id="ws-dot"></span>
      <span id="ws-text">연결 중...</span>
    </span>
  </div>
</div>
```

**배너 요소 추가** — `</div><!-- header -->` 바로 아래, `<div id="container">` 바로 위:

```html
<div id="banner" role="alert"></div>
```

**테이블 thead 수정** — 기존 5열 → 6열:

```html
<thead>
  <tr>
    <th>종목코드</th>
    <th>현재가</th>
    <th>고점</th>
    <th>하락률</th>
    <th>상태</th>
    <th>액션</th>
  </tr>
</thead>
```

**JavaScript 수정** — `<script>` 태그 내:

1. **전역 변수 추가** (최상단, 기존 `let ws;` 아래):
```javascript
var _lastSnapshot = {};
var bannerTimer = null;
```

2. **배너 함수 추가** (기존 `sanitizeCode` 함수 아래):
```javascript
function showBanner(msg, type) {
  var el = document.getElementById('banner');
  if (!el) return;
  el.textContent = msg;
  el.style.display = 'block';
  el.style.background = type === 'success' ? '#14532d' : '#7f1d1d';
  el.style.color = type === 'success' ? '#86efac' : '#fca5a5';
  if (bannerTimer) clearTimeout(bannerTimer);
  bannerTimer = setTimeout(function() { el.style.display = 'none'; }, 4000);
}
```

3. **드라이런 배지 갱신 함수** 추가:
```javascript
function updateDryRunBadge(isDryRun) {
  var badge = document.getElementById('dry-run-badge');
  if (!badge) return;
  badge.textContent = isDryRun ? '드라이런' : '실매도';
  badge.style.background = isDryRun ? '#78350f' : '#7f1d1d';
  badge.style.color = isDryRun ? '#fbbf24' : '#f87171';
}
```

4. **API 호출 핸들러** 추가:

```javascript
function startBot() {
  fetch('/api/start', {method: 'POST'})
    .then(function(r) {
      return r.json().then(function(body) { return {ok: r.ok, body: body}; });
    })
    .then(function(res) {
      if (!res.ok) showBanner(res.body.detail || '시작 실패', 'error');
    })
    .catch(function(e) { showBanner('요청 실패: ' + e, 'error'); });
}

function stopBot() {
  var hasPending = Object.keys(_lastSnapshot).some(function(k) {
    return k !== '_meta' && _lastSnapshot[k] && _lastSnapshot[k].sell_pending;
  });
  if (hasPending) {
    if (!confirm('매도 진행 중인 종목이 있습니다. 그래도 중지하시겠습니까?')) return;
  }
  fetch('/api/stop', {method: 'POST'})
    .then(function(r) { return r.json(); })
    .catch(function(e) { showBanner('요청 실패: ' + e, 'error'); });
}

function toggleDryRun() {
  fetch('/api/toggle-dry-run', {method: 'POST'})
    .then(function(r) { return r.json(); })
    .then(function(body) {
      if (body.ok) showBanner(body.message, 'success');
      else showBanner(body.message || '토글 실패', 'error');
    })
    .catch(function(e) { showBanner('요청 실패: ' + e, 'error'); });
}

function manualSell(code) {
  if (!confirm('[' + code + ']을(를) 시장가로 매도하시겠습니까?')) return;
  fetch('/api/sell/' + code, {method: 'POST'})
    .then(function(r) {
      return r.json().then(function(body) { return {ok: r.ok, body: body}; });
    })
    .then(function(res) {
      if (res.ok) showBanner(res.body.message || '매도 주문 제출됨 — 체결 확인 필요', 'success');
      else showBanner('매도 실패: ' + (res.body.detail || res.body.message || '오류'), 'error');
    })
    .catch(function(e) { showBanner('요청 실패: ' + e, 'error'); });
}
```

5. **renderTable 함수 수정** — `_meta` 키 필터링 + 6번째 열(즉시 매도 버튼) 추가:

기존 `function renderTable(data)` 전체를 아래로 교체:
```javascript
function renderTable(data) {
  var tbody = document.getElementById('symbol-table-body');
  var emptyState = document.getElementById('empty-state');
  var table = document.getElementById('symbol-table');

  // _meta 키 제외 — Pitfall 4 방어
  var symbols = {};
  Object.keys(data).forEach(function(k) {
    if (k !== '_meta') symbols[k] = data[k];
  });

  var keys = Object.keys(symbols);
  if (keys.length === 0) {
    tbody.innerHTML = '';
    table.style.display = 'none';
    emptyState.style.display = 'block';
    return;
  }
  table.style.display = '';
  emptyState.style.display = 'none';
  tbody.innerHTML = keys.map(function(code) {
    var sym = symbols[code];
    var isPending = sym.sell_pending;
    var dropColor = getDropColor(sym.drop_pct);
    var safeCode = sanitizeCode(sym.code);
    var sellBtn = '<button class="sell-btn" onclick="manualSell(\'' + safeCode + '\')" ' +
      (isPending ? 'disabled' : '') + '>즉시 매도</button>';
    return '<tr class="' + (isPending ? 'sell-pending' : '') + '">' +
      '<td>' + safeCode + '</td>' +
      '<td>' + fmtPrice(sym.current_price) + '</td>' +
      '<td>' + fmtPrice(sym.peak_price) + '</td>' +
      '<td style="color:' + dropColor + '">' + fmtDrop(sym.drop_pct, sym.warm) + '</td>' +
      '<td><span class="status-badge">' + getStatusText(sym) + '</span></td>' +
      '<td style="text-align:center;">' + sellBtn + '</td>' +
      '</tr>';
  }).join('');
}
```

6. **ws.onmessage 수정** — `_meta` 기반 버튼 상태 갱신:

기존 `ws.onmessage` 전체를 아래로 교체:
```javascript
ws.onmessage = function(e) {
  try {
    var data = JSON.parse(e.data);
    _lastSnapshot = data;
    var meta = data._meta || {};
    var isRunning = meta.is_running || false;
    var isDryRun = meta.dry_run !== undefined ? meta.dry_run : true;

    var btnStart = document.getElementById('btn-start');
    var btnStop = document.getElementById('btn-stop');
    if (btnStart) btnStart.disabled = isRunning;
    if (btnStop) btnStop.disabled = !isRunning;

    updateDryRunBadge(isDryRun);
    renderTable(data);
  } catch(err) { console.error('parse error', err); }
};
```
  </action>

  <verify>
    <automated>python3.11 -m pytest tests/ -x -q</automated>
  </verify>

  <acceptance_criteria>
    - index.html에 `id="btn-start"` 요소가 존재한다
    - index.html에 `id="btn-stop"` 요소가 존재한다
    - index.html에 `id="dry-run-badge"` 요소가 존재한다
    - index.html에 `id="banner"` 요소가 존재한다 (role="alert")
    - index.html에 `function startBot()` 함수가 존재한다
    - index.html에 `function stopBot()` 함수가 존재한다
    - index.html에 `function toggleDryRun()` 함수가 존재한다
    - index.html에 `function manualSell(` 함수가 존재한다
    - index.html에 `function showBanner(` 함수가 존재한다
    - index.html에 `function updateDryRunBadge(` 함수가 존재한다
    - index.html의 renderTable에 `k !== '_meta'` 필터가 존재한다
    - index.html의 thead에 6개 `<th>` 태그가 존재한다 (마지막은 "액션")
    - index.html의 ws.onmessage에 `data._meta` 참조가 존재한다
    - index.html의 stopBot에 `매도 진행 중인 종목이 있습니다` confirm 메시지가 존재한다
    - index.html의 manualSell에 `시장가로 매도하시겠습니까` confirm 메시지가 존재한다
    - `python3.11 -m pytest tests/ -x -q` 전체 통과 (기존 테스트가 깨지지 않음)
  </acceptance_criteria>

  <done>대시보드에 시작/중지 버튼, 드라이런 배지, 배너, 즉시 매도 버튼이 모두 표시되고, WebSocket 스냅샷 기반으로 실시간 상태가 갱신되며, 전체 테스트 스위트 GREEN</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
브라우저에서 봇 제어 UI가 동작하는지 확인합니다. Plan 01에서 추가된 4개 REST API와 이번 Task에서 추가된 프론트엔드 UI의 통합을 검증합니다.
  </what-built>
  <how-to-verify>
1. 터미널에서 `python3.11 -m mutrade` 또는 `uvicorn mutrade.admin.app:create_app --factory` 로 서버 시작
2. 브라우저에서 `http://localhost:8000` 접속
3. 확인 항목:
   - 헤더에 "시작" 버튼(파란색), "중지" 버튼(빨간색, disabled), "드라이런" 배지(노란색)가 보이는지
   - "시작" 버튼 클릭 → 시장 시간 외라면 상단에 빨간 배너 "시장 시간이 아닙니다 (09:00~15:20 KST)" 표시 (4초 후 자동 숨김)
   - "드라이런" 배지 클릭 → 상단에 초록 배너 "실매도 모드로 전환됨..." 표시, 배지가 "실매도"(빨간 배지)로 변경
   - 다시 배지 클릭 → "드라이런 모드로 전환됨..." 배너, 배지 원복
   - 종목이 표시된 경우 각 행 마지막 열에 "즉시 매도" 버튼이 보이는지
  </how-to-verify>
  <resume-signal>"approved" 또는 발견된 문제 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JS -> REST API | fetch()로 POST 요청 전송 |
| WebSocket -> JS renderTable | 서버 스냅샷 데이터를 innerHTML에 삽입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-05 | Tampering | index.html innerHTML 삽입 | mitigate | sanitizeCode() 함수로 종목코드에서 영숫자 외 문자 제거. 기존 Phase 6 패턴 유지. |
| T-7-06 | Information Disclosure | confirm 다이얼로그 종목코드 노출 | accept | 개인용 봇, 동일 브라우저에서만 접근. |
</threat_model>

<verification>
1. `python3.11 -m pytest tests/ -x -q` — 전체 스위트 GREEN
2. index.html에 btn-start, btn-stop, dry-run-badge, banner, manualSell 존재 (grep 확인)
3. 브라우저 수동 확인 (checkpoint)
</verification>

<success_criteria>
- 헤더에 시작/중지 버튼과 드라이런 배지가 표시됨
- 배너가 API 응답에 따라 성공(초록)/실패(빨강) 표시 후 4초 자동 숨김
- WebSocket 스냅샷의 is_running에 따라 시작/중지 버튼 disabled 토글
- WebSocket 스냅샷의 dry_run에 따라 배지 텍스트/색상 갱신
- 테이블 6열에 즉시 매도 버튼, SELL_PENDING 행은 disabled
- 즉시 매도 클릭 시 confirm 다이얼로그 → 확인 시 API 호출 → 배너 피드백
- 중지 클릭 시 SELL_PENDING 있으면 confirm 다이얼로그
</success_criteria>

<output>
After completion, create `.planning/phases/07-봇-제어/07-02-SUMMARY.md`
</output>
