---
phase: quick
plan: 260410-1dv
type: execute
wave: 1
depends_on: []
files_modified:
  - mutrade/kis/client.py
autonomous: true
requirements:
  - D-03
must_haves:
  truths:
    - "KIS_MOCK=true 시 PyKis가 모의투자 계좌번호(KIS_VIRTUAL_ACCOUNT)로 초기화된다"
    - "KIS_MOCK=false 시 동작 변경 없음 — 실전 계좌번호(KIS_ACCOUNT) 그대로 사용"
  artifacts:
    - path: "mutrade/kis/client.py"
      provides: "올바른 account 파라미터 분기"
  key_links:
    - from: "create_kis_client()"
      to: "PyKis.__init__(account=...)"
      via: "settings.kis_virtual_account (mock) / settings.kis_account (real)"
---

<objective>
KIS_MOCK=true일 때 `create_kis_client()`가 `account=settings.kis_virtual_account`를
전달하도록 수정하여 INVALID_CHECK_ACNO 오류를 해소한다.

Purpose: pykis 2.1.6에서 `account` 파라미터가 primary_account로 설정되며,
모의투자 엔드포인트(VTTC*)는 실전 계좌번호를 거부한다.

Output: mutrade/kis/client.py 1줄 수정 + 단위 테스트 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@mutrade/kis/client.py
@mutrade/settings.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mock 분기 account 파라미터 수정 및 테스트</name>
  <files>mutrade/kis/client.py, tests/kis/test_client.py</files>
  <behavior>
    - Test 1: KIS_MOCK=true 설정으로 create_kis_client() 호출 시 PyKis가
      account=settings.kis_virtual_account 값으로 초기화된다
    - Test 2: KIS_MOCK=false 설정으로 호출 시 PyKis가
      account=settings.kis_account 값으로 초기화된다 (기존 동작 유지)
  </behavior>
  <action>
    1. tests/kis/test_client.py 작성 (없으면 신규 생성):
       - unittest.mock.patch("mutrade.kis.client.PyKis")로 PyKis 생성자를 Mock
       - mock=True 케이스: 호출 kwargs 중 account == settings.kis_virtual_account 검증
       - mock=False 케이스: account == settings.kis_account 검증
       - Settings는 실제 인스턴스 대신 MagicMock으로 주입 (환경변수 불필요)

    2. 테스트 실행 → RED 확인 (mock=True 케이스에서 실패해야 정상)

    3. mutrade/kis/client.py 수정:
       mock 분기(line 32-41)의 `account=settings.kis_account` →
       `account=settings.kis_virtual_account` 로 변경 (D-03 per)

    4. 테스트 재실행 → GREEN 확인

    주의: kis_virtual_account는 Settings.validate_virtual_credentials에서
    KIS_MOCK=true 시 None이 아님이 보장되므로 None 체크 불필요.
  </action>
  <verify>
    <automated>python -m pytest tests/kis/test_client.py -v</automated>
  </verify>
  <done>
    - mock=True 케이스: PyKis 생성자에 account=kis_virtual_account 전달 확인
    - mock=False 케이스: account=kis_account 전달 유지 확인
    - 두 테스트 모두 PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env→Settings | 계좌번호 오설정 시 잘못된 엔드포인트로 주문 발생 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1dv-01 | Spoofing | create_kis_client | accept | Settings.validate_virtual_credentials가 KIS_MOCK=true 시 모든 가상 자격증명 존재를 이미 검증함 |
</threat_model>

<verification>
pytest tests/kis/test_client.py -v → 전체 PASS
</verification>

<success_criteria>
- mutrade/kis/client.py mock 분기: account=settings.kis_virtual_account
- KIS_MOCK=true 실행 시 INVALID_CHECK_ACNO 오류 미발생
- 기존 real 모드 동작 변경 없음
</success_criteria>

<output>
After completion, create `.planning/quick/260410-1dv-client-py-create-kis-client-kis-mock-tru/260410-1dv-SUMMARY.md`
</output>
